/**
 * Felles visnings-konstanter for spiller-velgeren (#435).
 *
 * `PENDING_PLAYER_LABEL` er fjernet herfra — teksten lever nå i katalogen
 * under `wizard.sections.players.pendingLabel` (no/en). Konsumenter bruker
 * `useTranslations('wizard.sections.players')` og slår opp `'pendingLabel'`
 * for å hente den lokaliserte etiketten.
 */
