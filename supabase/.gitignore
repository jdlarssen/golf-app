# Supabase CLI local runtime artefacts — generated by `supabase start` / `supabase test db`.
# Never committed; the rig (supabase/tests/) + config.toml are the only tracked Supabase files.
.branches
.temp
.env
