# Spec: Seks oppfølgingsfunn fra dagens runder — tabIndex, registrert-telling, scramble-sortering, kollaps-test, død BasicsSection-blokk, state 3.5-segment

**Issues:** #1666 · #1662 · #1667 · #1657 · #1660 · #1661 · **Branch:** claude/oppfoelging-1666-1662-1667-1657-1660-1661

Seks små, presist avgrensede funn filet av evaluatorer/byggere i dag (PR #1658/#1659/#1663/#1664).
Ingen produktvalg. Én commit per issue, `Refs #N`; PR-body `Closes #N` per issue.
`fix`-commits (#1666, #1662, #1661) får `.changes/`-notat; #1667/#1660 = `refactor`,
#1657 = `test`. Staging-verifisering av #1662 og #1661 før merge.

## Design per issue

### #1666 — SpecificValueSheet: `tabIndex={-1}` på containeren (fix)
`components/hole/SpecificValueSheet.tsx`: elementet med `ref={containerRef}` (fra
`useModalFocus`, ca. :103) får `tabIndex={-1}` som de andre konsumentene, så hookens
`container.focus()`-fallback virker når alt inni er disabled. Én linje + evt. én assert i
eksisterende SpecificValueSheet-test hvis den finnes (ikke ny fil).
`.changes/1666-score-ark-fokus.md`: «Score-arket beholder tastaturfokus også mens det
venter på lagring.»

### #1662 — «registrert»-tallet: én definisjon (fix)
`app/[locale]/admin/TilesGrid.tsx` teller alle `users`; `app/[locale]/admin/spillere/page.tsx:30–34`
teller `.not('profile_completed_at','is',null).is('deleted_at',null)`. Flisen får identisk
kjede + kryssreferanse-kommentar (samme form som #1615-fiksen i samme fil).
`.changes/1662-registrert-telling.md`: «Klubbhus-flisen og spillerlista teller nå spillere likt.»

### #1667 — scramble-views: dropp defensiv sortering (refactor)
`app/[locale]/games/[id]/leaderboard/formats/TexasScrambleView.tsx:87` og
`ShambleView.tsx:130`: fjern `[...result.teams].sort(...)`, les `result.teams` direkte
(compute-kontrakten er rangert siden #1591/#1574 — les doc-kommentaren på
`TexasScrambleResult.teams`/`ShambleResult.teams` og bekreft). Render-testene fra #1573 er
nett. Oppdater evt. kommentar som begrunnet sorteringen.

### #1657 — HoleClient-test vokter over-kollaps (test)
`app/[locale]/games/[id]/holes/[holeNumber]/HoleClient.test.tsx`: sjekk FØRST om
`lib/scoring/modes/types.test.ts` (eller tilsvarende) dekker `modeCollapsesToTeamCard`-terskelen
(patsome hull 6 → false, 7 → true; individuelle formater → false). Mangler den: legg Type A-
cases der (billigst, regelens hjem). I HoleClient-testen: én ny `it` (Type C) som rendrer et
individuelt format (f.eks. `stroke_play`/`stableford` slik eksisterende tester rigger) og
asserter at «mitt kort» er spillerens eget (oppslag på `myUserId`, ikke lag-kort) — velg en
assert som blir RØD hvis `isTeamCollapsedMode` tvinges til `true` (verifiser med en
midlertidig mutasjon, revert etterpå).

### #1660 — slett død `showAdvancedInline`-blokk (refactor)
`app/[locale]/admin/games/new/sections/BasicsSection.tsx:32,63,197–260`: begge call-sites
(`GameForm.tsx:623`, `GameWizard.tsx:864`) sender `false`. Fjern prop-en, default-en og
blokken (visibility-radioer + sideturnering-fieldset som ligger bak den). Fjern da-ubrukte
imports/props (`initialScoreVisibility`/`lockScoreVisibility`/side-*-props inn til
BasicsSection HVIS de kun brukes av blokken — grep i fila). Oppdater kommentaren i
`GameForm.tsx:613` som omtaler prop-en. Fjern `showAdvancedInline={false}` fra begge
call-sites. Sjekk `git log -S showAdvancedInline` for historikk og nevn i commit-body.
Tester: `GameForm.test.tsx`/`GameWizard.test.tsx` — hvis noen test dekker blokken (grep
`showAdvancedInline` i tester = 0 treff per nå), fjern den også.

### #1661 — leaderboard state 3.5 følger hull-segmentet (fix)
`app/[locale]/games/[id]/leaderboard/formats/state3.tsx` `renderState35` (:282–):
- Ny param `holeSegment: HoleSegment` (fra `game.hole_segment` i
  `leaderboardContent.tsx:530`-kallet).
- Klipp med `firstHalfHoleNumbersForSegment(holeSegment)` (`lib/games/holeScope.ts`) i stedet
  for hardkodet 1–9 (for `full` → [1..9], byte-identisk).
- Copy: `leaderboard.state35.lockedHeading` parametriseres «🤫 Vi sees ved hull {last}.» /
  "🤫 See you at hole {last}." med `last = lastHoleForSegment(holeSegment)`; `lockedSub`
  uendret. Pillen: `front9Label` («FRONT 9») beholdes for `full`; for segment-spill ny nøkkel
  `leaderboard.state35.segmentHalfLabel` «HULL {from}–{to}» / "HOLES {from}–{to}" med
  from/to fra første-halvdel-lista. Begge locales.
- Ingen testfil for state3 finnes; §Domain: nye placeholders → evaluator rendrer ruta (0
  MISSING_MESSAGE). `.changes/1661-tavle-segment.md`: «På en splittet cup-dag viser
  tavla riktig hvilke hull som er med før resten avsløres — ikke «hull 18» når runden slutter på 9.»

## Success Criteria
- [x] #1666: containeren har `tabIndex={-1}`. — Evidens: staging punkt 3 (`tabindex="-1"` på dialogen); test rød uten (mutasjon); commit 7a9aca97.
- [x] #1662: flis og /admin/spillere teller likt (staging: samme tall). — Evidens: staging punkt 1 (34 = 34; SQL filtrert 34, ufiltrert 37); commit 39f92ab6.
- [x] #1667: ingen `.sort(` på `result.teams` i de to viewene; leaderboard-suiten grønn. — Evidens: grep 0 sort på result.teams i begge views (Texas etter rebase på post-#1664 main); leaderboard-suite grønn; commit 8c1b15f1.
- [x] #1657: ny HoleClient-case rød ved tvungen `isTeamCollapsedMode = true`, grønn ellers; terskel-cases i types-test. — Evidens: mutasjon isTeamCollapsedMode=true → 1 rød (den nye), terskel-cases i familyPredicates.test.ts:235–248; commit 91a0695e.
- [x] #1660: `grep -rn showAdvancedInline app` = 0; wizard-/GameForm-suiter grønne. — Evidens: grep showAdvancedInline app = 0; −174 linjer; wizard/GameForm-suiter grønne; commit 5a92daae.
- [x] #1661: front9-spill i state 3.5 viser «hull 9» + «HULL 1–5»; full uendret («hull 18», «FRONT 9»); 0 MISSING_MESSAGE. — Evidens: staging punkt 2 (front9 «HULL 1–5» + «hull 9», full «FRONT 9» + «hull 18», 0 MISSING_MESSAGE); commit c0323230.
- [x] `.changes/`-notater parser; `npm run build`, `npm run lint`, co-located vitest grønt. — Evidens: weekly-release --dry-run OK; BUILD_EXIT=0; LINT 0 errors; 149 filer/1841 tester.

## Gates
- [x] `npx vitest run components/hole "app/[locale]/admin" "app/[locale]/games/[id]/leaderboard" "app/[locale]/games/[id]/holes" lib/scoring/modes` — Evidens: se evalueringsrapport (ACCEPT).
- [x] `npm run build` · `npm run lint` — Evidens: se evalueringsrapport (ACCEPT).
- [x] Staging: #1662 (flis vs header), #1661 (front9-spill i state 3.5 — trenger et aktivt best-ball-spill med `hole_segment='front9'` og score_visibility live; finn/rigg som i #1602-runden) — Evidens: se evalueringsrapport (ACCEPT).

## Out of Scope
- #1665 (innboks 0-rads + banner) — eget løp; #1653; #1525.
