# Spec: Interne restanser — testid, kollaps-regel, to tester, loop-doc

**Issues:** #1438 · #1606 · #1609 · #1524 · #1617 · **Branch:** claude/interne-restanser-1438-1606-1609-1524-1617

Fem builder-/reviewer-funn uten bruker-synlig effekt, samlet i én PR (presedens:
docs-trio-1566-1626-1403). Ingen produktvalg. Hvert issue får sin egen atomiske commit
med `Refs #N`; PR-body `Closes #N` per issue.

## Problem / Design per issue

### #1438 — `data-testid` på resend-knappen
`app/[locale]/(auth)/login/_components/VerifyCodeForm.tsx` `ResendFooter` (`<button type="submit">`, ca. :133)
mangler `data-testid`; søsken `change-email-link` har det. Legg til `data-testid="resend-code-button"`.
Ingen copy-endring. Prefiks `test:` (`[no-changelog]` unødvendig for `test:`).

### #1606 — HoleClient adopterer `modeCollapsesToTeamCard`
`app/[locale]/games/[id]/holes/[holeNumber]/HoleClient.tsx:408–409`:
```ts
const isTeamCollapsedMode =
  isTexas || isAlternateShotMatchplay(gameMode) || (isPatsome && currentHole >= 7);
```
→ `const isTeamCollapsedMode = modeCollapsesToTeamCard(gameMode, currentHole);`
(regelens hjem: `lib/scoring/modes/types.ts:182`). Fjern da-ubrukte `isPatsome` (:401) og
`isAlternateShotMatchplay`-importen (:40) HVIS de ikke brukes andre steder i fila (grep
viste kun disse linjene; `isTexas` brukes videre på :891 og skal stå). Oppdater kommentaren
over (:402–407) til å peke på helperen. Prefiks `refactor:`.

### #1609 — SubmitForm render-test (Type C, én)
Ny `app/[locale]/games/[id]/submit/SubmitForm.test.tsx`. Mock `dexie-react-hooks`
`useLiveQuery` (kjør callback? — nei: mock `@/lib/sync/db` sitt `localDb.syncQueue` er
tungt; enklere: mock `dexie-react-hooks` slik at `useLiveQuery(fn)` kaller `fn()` mot en
kontrollert `localDb.syncQueue`-mock med `.filter(pred).count()` som teller kø-elementer
som passerer `pred`). Mock også `@/lib/sync/syncWorker` (`drainQueue`) og `@/i18n/navigation`
(`useRouter` → `{ refresh: vi.fn() }`), og gi next-intl-meldinger via eksisterende
test-mønster (se hvordan andre `*.test.tsx` under `app/` gjør `NextIntlClientProvider`
eller mock av `useTranslations`). Ett `it`: mount med kø-element for eget spill →
`submit-scorecard` disabled; mount med kø-element for urelatert spill → enabled. Bruk
`isBlockingItem` ekte (ikke mock) så testen dekker koblingen. Maks én render-test.

### #1524 — cron-sweepens to-fase-orkestrering (Type A)
Ny `app/api/cron/start-scheduled-games/route.test.ts`. Mock `@/lib/supabase/admin`
(`getAdminClient` → chainable stub for `from('games').select().eq().lte().gte().returns()`
og `from('game_players').select().eq().is().returns()`), `@/lib/games/startScheduledGame`,
`@/lib/games/syncDerivedGamesStatus` (`startDerivedGames` no-op), `@/lib/notifications/events`
(`notifyPlayersGameStarted` spion), `@/lib/notifications/autoStartBlocked`, `next/cache`
(`revalidateTag` no-op). Sett `process.env.CRON_SECRET` og send `Authorization: Bearer`.
Bruk EKTE `pickStartNotificationTargets`. Tre cases fra issuet:
1. splittet cup-dag: 4 due spill (greensome-host `hole_segment:'full'`/`source_game_id:null`
   + 3 avledede med `source_game_id` satt — les `lib/notifications/startNotificationTargets.ts`
   for hva som faktisk gjør et spill til «det ene» per spiller, og bygg fixturen så
   nøyaktig ett `notifyPlayersGameStarted`-kall skjer og peker på det forventede spillet).
2. `startScheduledGame` → `{ ok: true, started: false }` → ingen varsel.
3. `startScheduledGame` kaster på spill nr. 2 → `POST` rejecter/kaster, MEN
   `notifyPlayersGameStarted` er kalt for spill nr. 1 (finally-passet).
Les `startScheduledGame`s retur-type før du skriver fixturen (I1).

### #1617 — loop-doc
`docs/loops/discord-pr-kort.md`: kort avsnitt (under «Hva Action-en gjør» steg 2 eller
«Tre utfall» punkt 1) om (a) at kortets egen `post-card`-check ekskluderes fra
grønt/rødt-klassifiseringen (`classifyChecks`, `CARD_CHECK_NAME`, både decide og
merge-endepunktets re-sjekk), og (b) ci.yml-gaten: PR-er med kode-endringer krever en
REGISTRERT ci.yml-run for head-SHA-en før grønt slippes (`expectsRealCi` /
`classifyWithCiGate`, #1520); docs-only uendret. Prefiks `docs:`.

## Success Criteria
- [x] #1438: `grep -n 'data-testid="resend-code-button"'` treffer i VerifyCodeForm.tsx. — Evidens: VerifyCodeForm.tsx:135, commit a5f78d63 (1 insertion).
- [x] #1606: HoleClient bruker `modeCollapsesToTeamCard`; ingen ubrukt `isPatsome`/`isAlternateShotMatchplay`; `npx vitest run app/\[locale\]/games/\[id\]/holes` grønt. — Evidens: HoleClient.tsx:40,408; grep isPatsome/isAlternateShotMatchplay = 0 treff; holes-suite grønn (evaluator: term-for-term ekvivalens, commit 9d9567ee).
- [x] #1609: `SubmitForm.test.tsx` finnes, én render-test, grønn. — Evidens: commit a47c9525, `Tests 1 passed`; mutasjonsbevist av evaluator (disabled=false → rød, belongsToGame→true → rød).
- [x] #1524: `route.test.ts` finnes, tre cases, grønn. — Evidens: commit f3a4f973 (+ roster-lookup-assert for punkt 3), `Tests 3 passed`; mutasjoner (push utenfor started-gate, drop finally) → rød per evaluator.
- [x] #1617: doc-avsnitt finnes med begge regler. — Evidens: docs/loops/discord-pr-kort.md «Hva «CI grønn» betyr (#1520)»; evaluator fant ingen faktafeil; `expectsRealCi(null)`-grenen lagt til etter funn 2.
- [x] `npm run build` grønt; `npm run lint` grønt. — Evidens: BUILD_EXIT=0, LINT_EXIT=0 (0 errors, 55 pre-eksisterende warnings).

## Gates
- [x] `npx vitest run <hver ny/endret testfil>` + co-located tester for endrede filer — 17 filer / 352 tester grønne (orkestrator), 13 filer / 151 (evaluator).
- [x] `npm run build` — exit 0
- [x] `npm run lint` — exit 0

## Out of Scope
- Copy-endringer, e2e-spec for resend, andre modaler/tester.
