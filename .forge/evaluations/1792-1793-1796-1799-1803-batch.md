# Evaluering: PR #1809 — batch K1/K2/K5/K8 (#1792, #1793, #1796, #1799) + #1803

**Dato:** 2026-08-30
**Evaluator:** frisk-kontekst forge-evaluator (skeptisk, falsifiseringsmandat)
**Kontrakt:** `.forge/contracts/1069-polish-bolgen.md` (K1/K2/K5/K8 + Edge Cases + P3/P4) og issue-teksten i #1803
**Evaluert HEAD:** `526cfd58` (lokal worktree; remote sto på `906f6455` under evalueringen — se merknad 1)

**Verdikt: ACCEPT**

## Kontraktkrav → evidens

| Krav | Verdikt | Evidens |
|---|---|---|
| **K1:** «both» fjernet som valgbart alternativ (wizard/edit) | PASS | `RegistrationSection.tsx:238–242` — `REGISTRATION_TYPES = ['solo','team']`; `typeTitle` uten both-gren. Edit-flyten deler samme komponent (GameForm/GameWizard). GameWizard-testen asserterer nå `queryByRole('radio', {name:/^begge$/i})` → null. |
| **K1:** DB-enum urørt, ingen migrasjoner | PASS | `git diff abc191bd..HEAD --stat`: 0 filer under `supabase/migrations/`. `lib/games/registration.ts` beholder `'both'` i type-union + `isRegistrationType` (bevisst — eksisterende data). |
| **K1:** eksisterende both-spill behandles som team i edit-flyt | PASS | `useGameFormState.ts:656–660` — `initialValues.registration_type === 'both' ? 'team' : …`. Dekker også restaurert wizard-draft (draft seedes via `initialValues`, `wizardStatePersistence.ts:97` + remount med `key='restored'`). |
| **K1:** solo-bakdøren LUKKET, ikke flyttet | PASS | `actions.ts:721–731` `isSoloRegistrationView` speiler `resolveRegistrationTypeView` + `gameModeSupportsTeams`; brukt i BEGGE actions (`registerForOpenGame` :215, `requestApproval` :425). Verifisert mot `registrationTypeView.ts`: both+lagmodus → `team_form` → avvist; team+alt → avvist; both+solomodus → `solo_form` → fortsatt eneste inngang (kravet «spillet må kunne meldes på» består). |
| **K1/P4:** hostile-POST-test, rød mot gammel kode | PASS | `actions.test.ts:639–649` (registerForOpenGame, both+best_ball → `team_not_supported_yet`), `:678–692` (requestApproval, samme). Rød-mot-gammel er statisk sikker: gammel gate var KUN `=== 'team'` — both+best_ball ville passert gaten, og ingen annen kodesti produserer `team_not_supported_yet`, så assertert objekt kan ikke matche. PR-body dokumenterer rød kjøring (2 failed \| 30 passed). Bevaringstesten (:651–669) låser both+stableford-solo-inngangen. |
| **K1:** meldingsfelt kollapset bak «Legg til melding», QR auto-ekspander | PASS | `RegistrationForm.tsx:44` `useState(src === 'qr')`; lenke med `data-testid="add-message-link"`, ≥44px (`min-h-[44px]`). Unit-tester for begge tilstander (`RegistrationForm.test.tsx:529–547`). QR-ekspander var byggerens skjønn — utøvd. |
| **K1:** i18n i BEGGE locales | PASS | `typeBothTitle` fjernet + `addMessageLink` lagt til i både `messages/no.json` og `messages/en.json`. Ingen foreldreløse `typeBoth`-nøkler igjen (grep). |
| **K1:** docs/flows-sjekk | PASS | `grep -rin "begge\|both" docs/flows/ docs/user-flows.md` → 0 treff → ingen diagram-endring kreves (matcher drift-tabellen i PR). |
| **K1:** e2e-specs oppdatert i samme PR | PASS | `e2e/signup/manual-approval.spec.ts` klikker `add-message-link` før fill (commit `906f6455`, etter rød CI-kjøring — CI-vakt-kommentar på PR). e2e-jobben SUCCESS etterpå. Asserterer testid, ikke norsk copy. |
| **K2:** confirm kun når hull mangler, dagens tekst beholdt | PASS | `SubmitForm.tsx:382` `if (missingHoles === 0) return;` — mangler-grenen bruker uendret `confirmBase + '\n\n' + confirmMissing`. Kø-låsen (#668) sjekkes FØR early-return, uendret. |
| **K2:** ~12px mer margin, targets ≥44px | PASS | `submit/page.tsx` gap-3→gap-6 (12→24px, +12px) i både ReviewBody og skeleton; knappene beholder `min-h-[44px]`/`h-11`. |
| **K5:** trykkflate ≥44px, visuell plassering uendret (#939) | PASS | `PuttsTogglePill.tsx`: knapp = usynlig flate (`padding: 9px 6px` speilet av `margin: -9px -6px` → null netto layoutskift), visuell pille flyttet uendret inn i indre span (~27px + 2×9px = ~45px). Staging-orakel: `[role=switch]` boundingBox ≥44px ✅ (role finnes på knappen, verifisert :31). |
| **K8:** KUN innboks-varianten flyttet | PASS | `innboks/page.tsx` — toggle nå ETTER `InboxClient`. Ingen filer under `app/[locale]/profile/` i diffen; grep over alle MonthlyDigest/product_updates-flater viser ingen andre flyttinger. #401 står. |
| **#1803:** delt primitiv, tre call-sites, testid bevart, ingen visuell endring | PASS | `components/ui/ShareLinkField.tsx` + én Type C-test; `CupShareLink`/`CopyJoinLinkButton`/`CopyShareLinkButton` er tynne adaptere. `cup-share-link`(+`-copy`) bevart via testId-prop — `e2e/cup/cup-lifecycle.spec.ts:822` matcher. Visuelle avvik begrenset til `min-w-0` på to felt (overflow-vern) — ikke merkbart. |
| **Tverrgående:** `.changes/`-notat per issue, riktig format | PASS | 4 notater (`1792/1793/1796/1799-*.md`), `type: fix` + `issue`, kropp ≤400 tegn. `node scripts/weekly-release.mjs --dry-run` parser alle uten feil (1.234.0 → 1.235.0). #1803 = refactor → korrekt uten notat. |
| **Tverrgående:** commit-disiplin + scope | PASS (m/ merknad 2) | Én fix-commit per issue med `Refs #N`; i tillegg to `[no-changelog]` test-følgecommits (#1792: `5b52fe55`, #1799: `526cfd58`) og én e2e-fiks (#1792: `906f6455`) — alle atomiske, alle i scope. Ingen filer utenfor K1/K2/K5/K8/#1803 + tester. Ingen eier-reversering blandet med ikke-reversering i samme commit. |
| **Gates** | PASS | `npx tsc --noEmit` → exit 0. `npx vitest run` (signup-mappa, SubmitForm, GameWizard, InboxClient, ShareLinkField) → 15 filer, 182 tester, alle grønne, exit 0. `npx eslint` på berørte filer → 0 errors (3 pre-eksisterende kompleksitets-advarsler, opplyst i PR-body). CI: verify/e2e/scan SUCCESS. |
| **Staging-bevis (P3/§T7)** | PASS | PR-kommentar med orakel-tabell: K1 rediger-skjema + meldingslenke m/ SQL-orakel, K2 begge grener m/ SQL-orakel, K5 boundingBox, K8 dokumentrekkefølge + DB-flip/tilbake, `scoring-golden-path @gate` grønn. Prod-vakt assertet staging-ref; testdata ryddet m/ SELECT-bevis. `staging-verified`-label satt. |

## Merknader (ikke blokkerende)

1. **Upushet commit:** lokal HEAD `526cfd58` (test(inbox), data-testid `monthly-digest-toggle`
   i produktfilen) var IKKE på remote under evalueringen (`ls-remote` → `906f6455`).
   PR-en er draft, så #1516-runbooken (push → `ls-remote`-sjekk → `gh pr ready` SIST)
   dekker dette — men den commiten MÅ pushes før ready, ellers merges PR-en uten
   testid-en staging-beviset og fremtidige innboks-verifiseringer lener seg på.
2. **«Én commit per issue»** ble i praksis fix-commit + test-følgecommits for #1792 og
   #1799. Atomisk og korrekt Refs-et; ingen kontraktsbrytelse, men PR-body-ens formulering
   («én commit per issue») er upresis.
3. K2-endringen (confirm-hoppet) har ingen unit-test — dekket av staging-orakel i stedet.
   Konsistent med test-disiplinens Type C-tak (én render-test per komponent finnes alt);
   nevnes kun for sporbarhet.
