# Evaluering — runde 3 (#1783 + #1782)

## PR A (#1783) — runde 1

**Verdikt: ACCEPT for T1.**

Evaluert commit: `c3ae648b` (refactor(cup): unify the form-parser result idiom on the
ok-flag form) på toppen av `335dd0c1`. Arbeidstreet var rent (`git status --porcelain`
tomt) da portene kjørte, så alle gate-utdata under gjelder nøyaktig den evaluerte
commit-en. Kun T1 er vurdert; T2 (#1782) er ikke bygget (out of scope), T3–T5 tilhører
hovedchatten.

### Gate-utdata (verbatim)

```
$ node -v
v22.23.0

$ npx vitest run lib/cup
VITEST_EXIT=0

 RUN  v4.1.6 /Users/jdl/Dokumenter/GitHub/golf-app/.claude/worktrees/mange-issues-a70214

 Test Files  37 passed (37)
      Tests  645 passed (645)
   Start at  22:06:23
   Duration  3.99s (transform 2.47s, setup 3.66s, import 1.58s, tests 1.78s, environment 22.10s)

$ npx tsc --noEmit
TSC_EXIT=0
(0 linjer utdata)

$ npx eslint lib/cup/
ESLINT_EXIT=0
(0 linjer utdata)

$ grep -rn "'error' in" lib/cup/
lib/cup/actions.ts:751:  if ('error' in plan) return { error: plan.error };
```

Merk om metode: første kjøring brukte `... | tail` og leste `$?` — som er `tail`s
exit-kode, ikke verktøyets (kjent falsk-grønn-felle). Alle tre portene over er kjørt på
nytt med utdata til fil og ekte exit-kode fanget direkte.

### Verifisering mot de fem punktene

**1. Shape-endring only — feilkoder og første-feil-rekkefølge uendret. BEKREFTET.**

Rekkefølgen på feilkodene i `parseCupPlanForm`, ekstrahert mekanisk fra begge
versjonene, er identisk:

```
gammel: 'plan_preset' 'plan_strategy' 'plan_sessions' 'plan_sessions'
ny:     'plan_preset' 'plan_strategy' 'plan_sessions' 'plan_sessions'
```

`plan_best_ball` kommer fra `parseBestBall`, og kallstedet ligger i begge versjoner
etter customSessions-blokka og før suksess-returen — posisjonen er uendret.

Hver endret linje i `planValidation.ts` er en `return`-setning eller en type-annotasjon.
Selve betingelsene er byte-identiske: `cleaned === ''`, `!Number.isInteger(n) || n < 0
|| n > 100` (kun klammer lagt til), `!VALID_PRESET_IDS.has(presetId)`,
`strategy !== 'handicap' && strategy !== 'random'`, og array/JSON-sjekkene. Den ene
betingelsen som med vilje er endret er diskrimineringen selv:
`'error' in bestBall` → `!bestBall.ok`.

Intern helper `parseBestBall` er unifisert i samme slengen
(`{ ok: number | null } | { error }` → `{ ok: true; value } | { ok: false; error }`), så
modulen ender ikke halvt-og-halvt slik kontrakten krever. Den nye eksporterte typen
`CupPlanParseResult` speiler søsken-idiomet `CupDraftParseResult`
(`parseCupDraftForm.ts:77-79`) felt for felt, inkludert `values`-navnet.

*Positivt funn (ingen handling):* gammel `parseBestBall` la tallet i `.ok`, så `0` var
en falsy suksessverdi — en `!x.ok`-sjekk mot den gamle formen ville vært en bug. Den nye
formen flytter tallet til `.value` og lar `.ok` være ren boolean, så kallstedets
`!bestBall.ok` er trygt. Dekket av den grønne `['0', 0]`-testen.

**2. Konsument-oppdateringen i `planActions.ts` — kun diskriminering og verdiaksess.
BEKREFTET.** Diffen har nøyaktig to hunks: `'error' in parsed` → `!parsed.ok`
(linje 180), og `parsed.ok.X` → `parsed.values.X` for de fire feltene i upsert-objektet.
Ingen DB-kall, ingen kolonnenavn, ingen kontrollflyt rørt — `onConflict`,
`updated_at`, guard-rekkefølgen (`plan_course`/`plan_tee` etter parse) står urørt.

**3. Test-endringene er mekaniske. BEKREFTET — og `toHaveProperty`-fella er håndtert.**

Den farlige omskrivingen var `toHaveProperty('ok')`: under den nye formen har også
feil-objektet en `ok`-nøkkel, så en bar `toHaveProperty('ok')` ville passert for
feiltilfeller og stilltiende svekket to tester. Byggeren skrev
`toHaveProperty('ok', true)` (linje 32 og 49) — nøyaktig like sterkt som før, ikke
svakere.

Øvrige endringer: `toEqual({ error: X })` → `toEqual({ ok: false, error: X })` (toEqual
er eksakt match, så styrken er bevart), og `ok: {…}` → `ok: true, values: {…}` i
suksess-assertene. Ingen forventet kode eller verdi er endret.

Ingen test er slettet eller lagt til — tellingene er identiske før og etter:

```
it(/it.each:  14 → 14
describe:      5 → 5
expect(:      15 → 15
.each-rader:   9 → 9
```

**4. Ingenting utenfor `lib/cup/`. BEKREFTET.** `git diff --name-only 335dd0c1..c3ae648b`
gir tre filer, alle under `lib/cup/`. Treff på `lib/loops`: 0.

**5. Scope mot kontraktens PR A-fil-liste. BEKREFTET.** Kontrakten lister
`planValidation.ts`, `planActions.ts`, planValidation-tester — commit-en rører de tre og
ikke mer. Commit-melding har `Refs #1783`. Ingen `.changes/`-notat, korrekt for
`refactor`.

### Funn

**NIT — `lib/cup/actions.ts:751` + T1 (bryter ikke kriteriet).**
`grep -rn "'error' in" lib/cup/` gir fortsatt ett treff:
`if ('error' in plan) return { error: plan.error };`. Dette er IKKE et parser-resultat —
`planCupMatchSwap` (`actions.ts:472`) returnerer
`Promise<SwapPlan | { error: string }>`, altså lese-/valideringsfasen for et
spillerbytte, ikke en skjema-parser. T1 krever «ingen treff på parser-resultater», og
det holder. Kontraktens problemformulering gjelder eksplisitt «to skjema-parsere», så
denne unionen er utenfor #1783. Verdt å vite at `'error' in`-idiomet dermed fortsatt
lever i `lib/cup/` for én ikke-parser-union; hvis eieren vil ha idiomet helt ensrettet
er det et eget, lite oppfølgingsissue — ikke noe som skal smyges inn i denne PR-en (I4).

**Ingen MUST-FIX.**

### Konklusjon

Ren shape-endring, ingen atferdsendring. Alle fire T1-portene grønne med ekte exit-kode
0: `npx vitest run lib/cup` (37 filer / 645 tester), `npx tsc --noEmit`,
`npx eslint lib/cup/`, og `grep "'error' in" lib/cup/` uten parser-treff.
Test-endringene er flagget som mekaniske og verifisert som det — inkludert den ene
assertion-en (`toHaveProperty`) som kunne blitt svekket, men ikke ble det.

## PR B (#1782) — runde 1

**Verdikt: ACCEPT for T2.**

Evaluert commit: `9c038945` (refactor(loops): decompose the Discord interactions
dispatcher) på toppen av `34a20116`. Arbeidstreet var rent da portene kjørte, og er rent
etterpå (evalueringen la ikke igjen artefakter). Kun T2 er vurdert; T3–T5 tilhører
hovedchatten.

Dette er merge-porten (`lib/loops/` = aldri-auto-merge-lista), så jeg tok ingenting på
tro. Byggerens to bevis er IKKE gjenbrukt — jeg reproduserte ekvivalensen med egen
metode (brace-matchet ekstraksjon + byte-eksakt literal-/kommentar-multimengde).

### Gate-utdata (verbatim)

```
$ node -v
v22.23.0

$ npx vitest run lib/loops app/api/discord
 Test Files  6 passed (6)
      Tests  281 passed (281)
VITEST_EXIT=0

$ npx vitest run lib/loops/discordActions.test.ts
 Test Files  1 passed (1)
      Tests  70 passed (70)

$ npx tsc --noEmit
TSC_EXIT=0
(0 linjer utdata)

$ npx eslint lib/loops scripts/loops
ESLINT_EXIT=0
(0 linjer utdata)

$ npx eslint --max-warnings 0 lib/loops scripts/loops
ESLINT_MAXWARN0_EXIT=0
```

**Metode-merknad (viktig):** `complexity` er konfigurert som `["warn", 25]`
(`eslint.config.mjs:93`). En warning gir exit 0, så `ESLINT_EXIT=0` alene beviser
INGENTING. To ekstra kontroller lukker hullet:

1. Porten er ikke vakuøs — warningen fantes faktisk på baseline. Jeg la
   `34a20116`-innholdet midlertidig på fil-stien og kjørte eslint:
   ```
   lib/loops/discordActions.ts
     156:8  warning  Async function 'executeAction' has a complexity of 44. Maximum allowed is 25  complexity
   ✖ 1 problem (0 errors, 1 warning)
   ```
   (Fila ble umiddelbart gjenopprettet; `git diff --quiet` bekreftet rent tre.)
2. På HEAD gir `--max-warnings 0` exit 0 — altså null warnings OG null errors i BÅDE
   `lib/loops` og `scripts/loops`. Ingen funksjon over 25, ingen ny warning noe sted.

### Verifisering mot de fem punktene

**1. `discordActions.test.ts` uendret. BEKREFTET — sterkere enn tom diff.**
`git diff 34a20116..9c038945 -- lib/loops/discordActions.test.ts` er tom, og SHA-256 er
identisk i begge versjoner: `49af6d8416bf8578b8538ec40ae924d908e48ef1b0c22ac59bce05b6b0bb4255`.
Sikkerhetsnettet er ekte: 70 tester, 33 `executeAction(`-kall, alle seks familiene
dekket (merge_pr 13 referanser, snooze/drop 9, answer 7, publish 6, ready 5), og
`it.skip`/`describe.skip`/`it.todo` = 0. Ingen test er stilltiende deaktivert.

**2. Kun `discordActions.ts` rørt. BEKREFTET.**
`git diff --name-only 34a20116..9c038945` gir én fil. `route.ts`, `autoMerge.ts`,
`prCard.ts` og `draftSweep.ts` er urørte — også målt mot `origin/main` (ikke bare mot
34a20116), så ingen tidligere commit på branchen har rørt dem heller.

**3. Uavhengig ekvivalenssjekk av de seks flyttede kroppene. BEKREFTET, to metoder.**

*Metode A — brace-matchet ekstraksjon.* Egen parser (hopper over strenger, template-
literaler og kommentarer) trakk ut hver `case '<kind>': { … }`-kropp fra
`git show 34a20116:…` og hver helper-kropp fra HEAD, normaliserte kun whitespace og
kommentarlinjer, og sammenliknet:

```
IDENTICAL  ready_issue         (6 kodelinjer, 311 tegn)   sha=f0f03a77f6fe
IDENTICAL  answer              (6 kodelinjer, 321 tegn)   sha=9a45319fb7fc
IDENTICAL  drop_issue         (20 kodelinjer, 971 tegn)   sha=ccb2d6c5cbc4
IDENTICAL  snooze_issue       (21 kodelinjer, 1088 tegn)  sha=ef30560c091d
IDENTICAL  merge_pr           (31 kodelinjer, 1679 tegn)  sha=d543e5137030
IDENTICAL  publish_lansering  (48 kodelinjer, 2031 tegn)  sha=ea7001c71636
```

*Metode B — byte-eksakt literal-multimengde.* Metode A normaliserer whitespace, og
kunne dermed skjult en endring INNI en norsk streng (f.eks. dobbelt mellomrom eller et
byttet tankestrek-tegn). Derfor trakk jeg ut hver eneste streng-/template-literal og
hver kommentar byte-eksakt fra begge versjoner og sammenliknet multimengdene:

```
== STRING/TEMPLATE LITERALS: before=134 after=141
  'merge_pr' 'ready_issue' 'answer' 'publish_lansering' 'drop_issue' 'snooze_issue': 3->4 hver
  'kind': 0->1
== COMMENTS: before=50 after=53
  + "// Én handler per custom_id-familie (#1782). Handlerne tar den SMALNEDE"
  + "// action-varianten, så hver kropp er ordrett den samme som da alt lå i"
  + "// executeActions switch — dispatchen under er ren delegering."
```

Dette er nøyaktig fingeravtrykket en ren flytting skal ha. De seks kind-literalene får
én ny forekomst hver — `ActionOf<'…'>` i helper-signaturen — og `'kind'` kommer fra
`Extract<DiscordAction, { kind: K }>`. **Ingen bruker-rettet norsk streng er lagt til,
fjernet eller endret med ett eneste tegn**, og alle 50 opprinnelige kommentarer er
bevart ordrett (de tre nye er kun forklaring av dekomponeringen). Merge-portens
`✅ PR #… er rebase-merget…` og publiseringens `📣 Publisert: «…»` er dermed beviselig
uendret, inkludert emoji og «»-tegn.

*Merge-porten spesifikt:* GitHub-kallene er identiske kall for kall —
`GET /repos/…/pulls/<n>`, `fetchCiRunsForSha`, `graphql markPullRequestReadyForReview`,
og `PUT /repos/…/pulls/<n>/merge` med `{ merge_method: 'rebase' }`. Rekkefølgen på de
fem avvisningsgrenene (ikke funnet → ikke åpen → CI-oppslag feilet → ingen kjøring →
ikke completed → ikke success) er posisjon for posisjon den samme, og
`ciRuns.reduce((a, b) => (b.id > a.id ? b : a))` står uendret. Ingen gren er myknet.

**4. Eksportert signatur, private helpers, ingen smuglet «fiks». BEKREFTET.**
Eksport-listen er identisk linje for linje før/etter (`diff` på alle `^export`-linjer →
ingen forskjell). `executeAction(action, gh, lansering?)` er uendret, og `route.ts:215`
(`content = await executeAction(action, gh, deps)`) trengte ingen endring. Ingen av de
seks helperne er eksportert. `grep` for `eslint-disable`, `@ts-ignore`,
`@ts-expect-error`, `@ts-nocheck` i fila: **ingen treff** — kompleksiteten er løst
strukturelt, ikke undertrykt.

**5. Dispatch-komplett, og signatur-/timestamp-koden urørt. BEKREFTET.**
Diffens første hunk starter på linje 149. Alt over — `verifyDiscordSignature`,
`isTimestampFresh`, `DiscordAction`-unionen, `parseCustomId`, `LanseringDeps`,
`extractLanseringProposal`, `GitHubClient` — er bokstavelig talt ikke rørt. Alle seks
custom_id-familiene parses fortsatt av samme uendrede `parseCustomId`.

Dispatch-wiringen er verifisert mekanisk (union → case → helper → `ActionOf`), ikke med
øyemål — en feilkobling (f.eks. `case 'answer'` → `handleMergePr`) ville gitt utslag:

```
OK  merge_pr          -> handleMergePr          narrows ActionOf<merge_pr>
OK  ready_issue       -> handleReadyIssue       narrows ActionOf<ready_issue>
OK  answer            -> handleAnswer           narrows ActionOf<answer>
OK  publish_lansering -> handlePublishLansering narrows ActionOf<publish_lansering>
OK  drop_issue        -> handleDropIssue        narrows ActionOf<drop_issue>
OK  snooze_issue      -> handleSnoozeIssue      narrows ActionOf<snooze_issue>
```

`default`-grenen er FRAVÆRENDE i både før og etter — switchen er uttømmende over
unionen, og en (typemessig umulig) ukjent `kind` faller ut av funksjonen og gir
`undefined` i begge versjoner. Uendret oppførsel, ikke et nytt hull.

**Await-regnskap:** `executeAction` hadde 18 `await` før. Etter: 0 i dispatchen + 18
fordelt på de seks helperne = **18 totalt**. Ingen `await` er lagt til eller fjernet.

**Om `return handleX(...)` i stedet for `return await handleX(...)`:** å returnere et
promise fra en `async`-funksjon adopterer det, så både resolved verdi og rejection
propagerer identisk til `route.ts:215`. Eneste forskjell er et par mikrotask-tikk —
ikke observerbart, verken i testene eller i produksjon.

**`lansering`-parameteren:** `executeAction` beholder `lansering?: LanseringDeps`;
helperen tar `lansering: LanseringDeps | undefined` (påkrevd, men undefined-bar).
Kallet `handlePublishLansering(action, gh, lansering)` er alltid gyldig, og
`if (!lansering)`-grenen evalueres på nøyaktig samme punkt i flyten som før.

### Funn

**Ingen MUST-FIX. Ingen NIT verdt å filere.**

Jeg lette spesifikt etter: myknede avvisningsgrener i merge-porten, endret rekkefølge på
GitHub-kall, endret status-sammenlikninger (`!== 200` / `!== 201` / `!== 404`), tapte
`catch`-blokker i publish-grenen, undertrykt lint, eksponerte helpers, og endret
bruker-copy. Ingen av delene forekommer.

### Konklusjon

Ren flytting, verifisert med to uavhengige metoder som fanger ulike feilklasser
(struktur vs. byte-eksakt copy). T2s tre krav er alle innfridd med ekte exit-kode 0:
eslint-warningen for `executeAction` er borte (bevist at den fantes på baseline, og
bevist null gjenværende warnings med `--max-warnings 0`), `npx vitest run lib/loops`
er grønn med `discordActions.test.ts` bit-identisk umodifisert (70/70), og `route.ts`
er urørt.

Merk for hovedchatten: T5 står fortsatt — PR-en SKAL forbli draft og merges av eieren.
`lib/loops/` er på aldri-auto-merge-lista, og denne evalueringen er ikke et mandat til
å merge.
