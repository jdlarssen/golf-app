# Evaluering: seks oppfølgingsfunn (#1678 #1685 #1677 #1422 #1425 #1525)

**Branch:** `claude/batch-b-1678-1685-1677-1422-1425-1525` @ `e4cd53b1` (7 commits over `origin/main` — kontrakt + én per issue)
**Kontrakt:** `.forge/contracts/1678-1685-1677-1422-1425-1525-tester-admin-invitasjoner.md`
**Evaluator:** fersk kontekst. Statisk lesning + fire mutasjonsprober + full gate-kjøring + staging-klikkrunde mot `torny-staging`. Ingen produkt- eller testfil endret permanent; alle prober reversert med `git checkout --`.

## Success criteria

| # | Kriterium | Verdict | Bevis |
|---|---|---|---|
| 1 | **#1678** `grep -c "tee_gender: 'M'"` = 0; CH-assert rød ved `'M'`-mutasjon; `getRatingForGender('M')` → null med test | PASS | `grep -c "tee_gender: 'M'" lib/games/startScheduledGame.test.ts` = **0** (og 0 treff i hele repoet). CH-asserten ligger i «non-matchplay mode (stableford) ignorerer side-guard» (`startScheduledGame.test.ts:263`) og regner riktig mot fiksturen: `VALID_TEE` slope 113 / CR 72,0 / par 72, hcp 10, allowance 100 % → CH 10. **To-lags mutasjonsprobe:** (a) kun fikstur → `'M'`: 1 failed/45 passed, faller på `ok: true` fordi `== null`-vakta nå fanger ukjent gender oppstrøms. (b) fikstur → `'M'` **og** `teeRating.ts` tilbake til `=== null` (verdenen før fiksen): `ok: true` står grønt og det er **CH-asserten** som faller — `expected { course_handicap: NaN } to deeply equal { course_handicap: 10 }`. Asserten er altså nøyaktig NaN-orakelet kontrakten ba om. `teeRating.test.ts:63` dekker `getRatingForGender(fullTee, 'M')` → `null`. T2-callerne: 13 call-sites, alle typet mot `Rating \| null` — tsc (exit 0) tvinger null-håndtering, så ingen caller kan anta non-null. |
| 2 | **#1685** konstant-modul med `satisfies`; flightActions bruker den; ny lockstep-`it` rød ved `created_at`-litteral | PASS | `app/[locale]/admin/games/[id]/flightPlayerColumns.ts` (plain `.ts`, ikke `'use server'`) med `FLIGHT_PLAYER_SELECT` / `FLIGHT_TEAM_SELECT` / `FLIGHT_PLAYER_ORDER`, alle `as const satisfies readonly GamePlayerColumn[]`. `flightActions.ts:62,72-73,206,216-217` bruker dem. **Tre prober:** (a) `.select('user_id, flight_number, withdrawn_at, created_at')` → 1 failed/18 passed, faller i den nye `it`-en (`Expected "…accepted_at" / Received "…created_at"`); (b) `.order('created_at', …)` → samme `it` rød (`expected [ 'accepted_at', 'user_id' ] to include 'created_at'`); (c) `'created_at'` lagt inn i konstantene → `tsc` rød med **TS2322** på begge konstantene (mot ren `tsc` = 0 linjer på HEAD). Begge gatene virker. Emitterte strenger er byte-identiske med de gamle litteralene (runtime-no-op). |
| 3 | **#1677** `carryPreservedModeConfigKeys` + tester; edit-action bærer `team_strokes_override` over; staging positiv + draft-negativ | PASS | `lib/games/modeConfigEdit.ts` + 5 Type A-cases. `edit/actions.ts:163` utvider select til `status, game_mode, mode_config`; `:202` skriver `carryPreservedModeConfigKeys(existing!.mode_config, payload.mode_config)`. **Prober:** kind-vakta fjernet → «bærer ingenting over når modusen er byttet» rød; edit-actionen tilbake til `mode_config: payload.mode_config` → den nye `edit/actions.test.ts`-casen rød (`team_strokes_override` mangler). **Staging:** to påfølgende ekte lagringer av en scheduled greensome (skrivingen bevist ved at navnet endret seg i DB) beholdt `{"team1":8,"team2":3}` og **hele JSON-en uendret** — `kind`/`team_size`/`teams_count`/`allowance_pct` identisk, ingen nøkkel tapt eller lagt til. Draft med samme nøkkel, format byttet greensome → Stableford i skjemaet, «Lagre utkast» → lagret `{"kind":"stableford","team_size":1,"points_table":"standard"}` — **null greensome-nøkler**. |
| 4 | **#1422** `pendingRequestView` + tester; staging captain_invited-peker + selv-sendt uendret | PASS | `pendingRequestView.ts` + 6 `it.each`-cases. **Probe:** «returner alltid `self_request`» → captain_invited-casen rød. **Staging (E2E_PLAYER, pending child-rad med `team_request_id`):** `[data-testid=captain-invited-pointer]` = **1**, knappen peker på `/signup/evalb422/team`. Norsk: «Du er invitert til et lag i dette spillet. Svar ja eller nei på lagsiden.» Engelsk (`/en/…`): «You've been invited to a team in this game. Accept or decline on your team page.» — **begge locales, 0 MISSING_MESSAGE**. Sette `team_request_id = NULL` → pekeren borte (0) og «Forespørsel sendt — du får varsel når arrangøren har bestemt seg.» tilbake. Skriveren av `team_request_id` er verifisert: kun kaptein-flytene (`teamActions.ts:412`, `:1020`), kapteinens egen rad er `null` (`:312`). |
| 5 | **#1425** to åpne invitasjoner → peker vist; kun arrangør → peker skjult + lag-skjema; `inviteHasPendingBanner` uendret | PASS | `resolveCertainTeamInvitation` i `team/captainLookup.ts` + 4 Type A-cases. **Probe:** `source !== 'invited_by'`-vakta byttet til ren null-sjekk → «gir null når kun arrangøren har invitert» rød. **Staging:** spill med **to** åpne uutløpte invitasjoner til samme e-post (arrangør + kaptein) — nøyaktig PGRST116-tilfellet — gir `[data-testid=team-invite-pointer]` = **1** med lenke til `/signup/evalb425/team`, og lag-skjemaet står fortsatt under. Slettet kaptein-invitasjonen → pekeren = **0**, «Du melder på et helt lag som kaptein» + «Lag-navn (3–40 tegn)» synlig. `hasPendingInvitation` (= `openInvitations.length > 0`) mates fortsatt til `inviteHasPendingBanner` (`page.tsx:558`) med samme «enhver invitasjon teller»-semantikk — og er strengt mer robust enn `.maybeSingle()`, som ga `false` ved to rader. |
| 6 | **#1525** grep-kriteriet = 0; solo-holes render-tester grønne uendret; putter bruker `leaderboardHref` | PASS | `grep -rn 'backHref={\`/games/${gameId}\`}' app/[locale]/games/[id]/leaderboard/holes/` = **0**. Commit `e4cd53b1` rører **20 filer, null testfiler** (9 views + 9 format-bodies + `page.tsx` + `putter/page.tsx`); alle 9 `*HolesView.test.tsx` er uendret og grønne i gaten. `leaderboardHref({gameId})` → `buildHref` uten team/mode/context → `query === ''` → `/games/${id}/leaderboard`, **byte-identisk** med den gamle litteralen. **Staging (admin, ferdige spill):** nassau, solo_strokeplay, acey_deucey og round_robin — uten `?from=` er første lenke `/games/<id>` (som før), med `?from=%2Fcup%2Fevalb-test` blir den `/cup/evalb-test`. 0 console-errors, 0 MISSING_MESSAGE på alle. |
| 7 | `.changes`-notater parser; build/lint/vitest grønt | PASS | Se Gates. Tre notater, alle `fix`, `type` + `issue` og én setning norsk brødtekst — nøyaktig malen i `.changes/README.md` (`title`/`link`/`cta` hører kun til `feat`). Folder inn i `1.233.0`. |

## Gates

| Gate | Kommando | Resultat |
|---|---|---|
| Unit (kontraktens sett + `messages`) | `npx vitest run lib/games "app/[locale]/admin/games/[id]" "app/[locale]/signup" "app/[locale]/games/[id]/leaderboard/holes" "app/[locale]/games/[id]/putter" messages` | **exit 0** — 96 filer / 1578 tester grønne (kjørt to ganger: før og etter alle prober) |
| Typecheck | `npx tsc --noEmit -p .` | **exit 0**, 0 linjer output |
| Build | `npm run build` (`set -o pipefail`) | **exit 0** |
| Lint | `npm run lint` | **exit 0** — 0 errors, 56 warnings (alle pre-eksisterende, se F1) |
| Ukesslipp | `node scripts/weekly-release.mjs --dry-run` | **exit 0** — `1.232.2 → 1.233.0`, alle tre notatene folder inn |
| Mutasjonsprobe #1678 (a) | `tee_gender: 'mens'` → `'M'` | 1 failed / 45 passed |
| Mutasjonsprobe #1678 (b) | `'M'` + `=== null` (pre-fiks) | CH-asserten faller på `NaN` mens `ok: true` står grønt |
| Mutasjonsprobe #1685 (select) | select-litteral m/ `created_at` | ny lockstep-`it` rød (1 failed / 18 passed) |
| Mutasjonsprobe #1685 (order) | `.order('created_at')` | samme `it` rød |
| Mutasjonsprobe #1685 (tsc) | `'created_at'` i konstantene | tsc **TS2322** på begge konstantene |
| Mutasjonsprobe #1677 (a) | kind-vakta fjernet | mode-bytte-casen rød (1 failed / 4 passed) |
| Mutasjonsprobe #1677 (b) | `mode_config: payload.mode_config` | edit-casen rød (1 failed / 12 passed) |
| Mutasjonsprobe #1422 | alltid `self_request` | captain_invited-casen rød (1 failed / 5 passed) |
| Mutasjonsprobe #1425 | `source`-vakta fjernet | «kun arrangøren»-casen rød (1 failed / 10 passed) |
| Uavhengig sweep-kontroll (#1685) | alle `.order('created_at'` i `lib/` + `app/` mot `Database`-typen | **32 call-sites, 14 tabeller, 0 avvik** — alle har `created_at` |

## Staging-bevis

Kjørt mot `torny-staging` (`snwmueecmfqqdurxedxv`) fra denne worktreen: `rm -rf .next`, deretter `next dev -p 3146` via node-wrapper som leser `.env.staging.local` inn i prosess-env-en. cwd verifisert med `lsof -a -p 96889 -d cwd` = `…/worktrees/forge-auto-issue-0715fa`. Innlogging via service-role `generate_link` → `email_otp` (8 siffer, `303` på POST /login) for både `E2E_PLAYER_EMAIL` og `E2E_ADMIN_EMAIL`. Playwright via Bash.

| Akseptansepunkt | Struktur-orakel | Feillogg | SQL-orakel |
|---|---|---|---|
| **#1677** — lag-slagene overlever redigering | `/no/admin/games/<id>/edit` på scheduled greensome: «Lagre endringer» aktiv, klikket to ganger (hver gang med navneendring som skrive-detektor) | 0 console-errors, 0 MISSING_MESSAGE | Før: `{"kind":"greensome_matchplay","team_size":2,"teams_count":2,"allowance_pct":100,"team_strokes_override":{"team1":8,"team2":3}}`. Etter lagring 1 og 2: **identisk JSON**, navnet endret (skrivingen landet, `waitedMs` 2172 / 2142) |
| **#1677 negativ** — draft bytter format bort fra greensome | Åpnet «Spillform»-disclosure (nativ `<summary>`), 8 formatfliser (`Stableford, Best ball, Matchplay, Slagspill, Texas scramble, Ambrose, Florida Scramble, Par`), valgte Stableford, «Lagre utkast» | 0 console-errors, 0 MISSING_MESSAGE | Etter: `{"kind":"stableford","team_size":1,"points_table":"standard"}` — **ingen `team_strokes_override`, ingen greensome-nøkler** |
| **#1422** — kaptein-invitert får vei videre | `/no/signup/evalb422`: `captain-invited-pointer` = 1, `a[href]` = `/signup/evalb422/team`. `/en/signup/evalb422`: samme, engelsk tekst | 0 console-errors, **0 MISSING_MESSAGE** (ny nøkkel finnes i begge locales) | Rigg: kaptein-rad (`is_team_captain=true`, `team_name='Testlaget'`) + child-rad for E2E_PLAYER med `team_request_id` → kapteinens id |
| **#1422 negativ** — selv-sendt forespørsel uendret | Etter `PATCH team_request_id=null` (200, 1 rad): peker = 0, banner «Forespørsel sendt — du får varsel når arrangøren har bestemt seg.» | 0 console-errors, 0 MISSING_MESSAGE | Rad restaurert etterpå (200, 1 rad) |
| **#1425** — peker overlever doble invitasjoner | `/no/signup/evalb425`: `team-invite-pointer` = 1 + lag-skjemaet under | 0 console-errors, 0 MISSING_MESSAGE | To åpne, uutløpte `invitations` på samme e-post + game_id (arrangør `created_at` = nå, kaptein `created_at` = −1t) — nøyaktig raden som ga PGRST116 før |
| **#1425 negativ** — usikkert lag → ingen peker | Etter `DELETE` av kaptein-invitasjonen (200, 1 rad): peker = 0, «Du melder på et helt lag som kaptein» + «Lag-navn (3–40 tegn)» + «Meld på» synlig | 0 console-errors, 0 MISSING_MESSAGE | Kun arrangør-invitasjonen igjen |
| **#1525** — nav-context i solo-«Hull for hull» | Fire ferdige spill (nassau, solo_strokeplay, acey_deucey, round_robin): uten param → første lenke `/games/<id>`; med `?from=%2Fcup%2Fevalb-test` → `/cup/evalb-test`. `h1` = «Hull for hull» i alle åtte lastene | 0 console-errors, 0 MISSING_MESSAGE, 0 ekte requestfailed | — (ren struktur-sjekk; ingen skriving) |

**Prod-vakt: 0 brudd.** Alle browser-initierte Supabase-kall gikk til `snwmueecmfqqdurxedxv.supabase.co` (22 i #1525-runden, resten i admin-runden); null kall til prod-ref-en. Server-siden er bevist mot staging på en sterkere måte: de fire rigg-spillene (`evalb677`, `evalb67d`, `evalb422`, `evalb425`) fantes **kun** på staging — read-only SELECT mot prod (`glofubopddkjhymcbaph`) ga `0` rader på de short_id-ene, og sidene rendret dem likevel. `requestfailed`-oppføringene er alle `net::ERR_ABORTED` på `notifications`-pollen og på navigasjoner driveren selv avbrøt — driver-artefakter, ikke app-feil.

### Opprydding

- Riggen er slettet i sin helhet: 4 `games`, 8 `game_players`, 3 `game_registration_requests`, 1 gjenværende `invitations` (den andre ble slettet som del av #1425-negativen). Kontrolltelling etterpå: `leftover_games` 0, `leftover_players` 0, `leftover_requests` 0, `leftover_invitations` 0.
- Ingen eksisterende staging-rad ble endret — alt ble bygget som nye rader, og de to midlertidige feltendringene (`team_request_id`, slettet invitasjon) lå inne i rigg-rader som uansett er borte nå. Ingen `notifications` ble skapt (verifisert: 0 rader med rigg-`gameId`).
- Dev-serveren er stoppet (port 3146 ledig).
- `git status` **helt rent**; alle ni mutasjonsprober reversert. Denne evalueringsfila er eneste nye fil.

## Findings

| # | Fil + kriterium | Alvorlighet | Funn + forslag |
|---|---|---|---|
| F1 | `app/[locale]/signup/[shortId]/page.tsx:99,427` (#1422/#1425) | Lav (stil) | De to eslint-`complexity`-advarslene på fila er **pre-eksisterende** (målt på `origin/main`: `PåmeldingPage` 39, `renderBody` 26), men branchen dytter dem til 42 og 27. `edit/actions.ts` er uendret på 38. Ingen nye advarselstyper, 0 errors — men `renderBody` er nå en 10-grens-kjede og en naturlig kandidat for et eget splitte-issue neste gang noen rører den. |
| F2 | commit `56d4e0c8` (#1685) | Info | Commit-body-en oppgir «30 call sites» i `.order('created_at')`-sweepen. Min uavhengige opptelling gir **32** (14 tabeller). Konklusjonen er den samme og verifisert: alle 32 sorterer på en tabell som faktisk har `created_at`. Kun tallet i brødteksten er upresist. |
| F3 | `.changes/1422-laginvitert-lenke.md` | Info | Notatet sier «Er du **satt på** et lag av en kaptein», mens banneret sier «Du er **invitert til** et lag». Kontrakten foreslo «invitert». Ikke feil — men to ord for samme handling i to flater brukeren ser innen ett minutt. Forslag: la notatet speile banneret. |
| F4 | `app/[locale]/signup/[shortId]/page.tsx:474` (#1422) | Info (kant) | `captain_invited`-grenen ligger **før** `gameLocked`/`signupsClosed`, så en kaptein-invitert spiller på et spill som har startet ser fortsatt «Gå til laget» i stedet for «Påmelding er stengt». Nøyaktig samme rekkefølge som `self_request` hadde før, altså **ingen regresjon** — nevnt for sporbarhet. |
| F5 | `app/[locale]/signup/[shortId]/team/page.tsx:171-190` (#1425) | Info | `team/page.tsx` ble ikke byttet til `resolveCertainTeamInvitation`. Byggerens begrunnelse er **bekreftet ved lesning**: siden skiller `picked === null` (ingen kaptein i spillet → `team-stop-no-captain`) fra `source !== 'invited_by'` (ukjent lag → `team-stop-unknown-team`), og helperen kollapser begge til `null`. Kontrakten gjorde byttet valgfritt («KAN … ikke tving»). Ryddeforslag hvis noen vil ha én regel ett hjem: la helperen returnere en diskriminert union i stedet for `null`. |

Ingen av funnene endrer verdikten — F1/F2/F3 er kosmetikk, F4/F5 er sporbarhets-notater.

## Kontrakt-avvik (bekreftet, alle akseptable)

1. **#1678 plassering av CH-asserten.** Kontrakten pekte på «fryser CH ved start»-casen «eller nærmeste»; asserten ligger i «non-matchplay mode (stableford) ignorerer side-guard» (`:263`). Probe (b) beviser at den fanger nøyaktig NaN-en kontrakten var ute etter.
2. **#1685 `FLIGHT_TEAM_SELECT` uten `accepted_at`.** Kontrakten skrev `FLIGHT_TEAM_SELECT = [...,'team_number',...]`; bygget beholder de fire kolonnene den gamle litteralen hadde. Riktig: `accepted_at` brukes kun til `order`, og PostgREST tillater sortering på en kolonne som ikke selekteres. Dokumentert i modulens doc-kommentar.
3. **#1425 kaptein-spørringen henter kun `user_id`.** Kontrakten sa «som `team/page.tsx:131-139`» (som henter `id, user_id, team_name, status`). `CaptainCandidate` er `{ user_id: string }` og call-sitet trenger bare `!= null`, så det smalere selectet er korrekt og billigere.
4. **#1425 `team/page.tsx` ikke byttet.** Se F5 — begrunnelsen holder, og kontrakten gjorde byttet valgfritt.
5. **#1677 issue-stien.** Issuet oppgir `admin/cup/[id]/edit/actions.ts`, som ikke finnes. Avviket er dokumentert i commit-body-en, slik kontrakten krevde.
6. **Evaluators metodeavvik (ikke byggerens):** kontrakten ba om «lagre uendret» på staging. En helt uendret lagring etterlater ingen observerbar DB-endring på en dev-server der action-redirecten henger, så jeg brukte en navneendring som skrive-detektor — samme `updateScheduledAction`, samme `mode_config`-skriving, og strengt sterkere bevis (to reelle skrivinger, begge bevart nøkkelen).

## VERIFICATION GAPs

- **Bokstavelig «lagre uendret»** (helt urørt skjema) er ikke drevet — se avvik 6. Kodestien for `mode_config` er identisk, og to ekte lagringer bevarte nøkkelen.
- **5 av 9 solo-«Hull for hull»-visninger** (skins, wolf, nines, bingo_bango_bongo, solo_stableford) er ikke drevet live. De fire som ble drevet oppfører seg riktig; de fem øvrige har nøyaktig samme én-linjes endring, uendrede og grønne render-tester, og tsc/build grønt.
- **`putter/page.tsx`** er ikke drevet live (krever et ferdig spill der innlogget bruker selv er deltaker). `leaderboardHref({gameId})` er verifisert ved lesning til å gi `/games/${id}/leaderboard` uten query — byte-identisk med de to litteralene den erstatter.

---

**Verdict: ACCEPT**
