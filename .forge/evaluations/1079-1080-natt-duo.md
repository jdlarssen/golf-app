# Evaluation: Natt-duoen (#1079 + #1080)

## Verdict

ACCEPT (criterion 6 explicitly PENDING FIRST USE, as the contract itself states)

## Criteria

- [x] **Gate — docs-only diff.** `git diff --stat origin/main..HEAD` touches only `.forge/contracts/1079-1080-natt-duo.md`, `docs/loops/ci-vakta.md`, `docs/loops/dok-avstemmeren.md`, `docs/loops/morgenbriefen.md`, `docs/loops/nattkjoreren.md`. No code files.
- [x] **Criterion 1 — nattkjoreren.md required elements.** All 8 present, verified by direct quote:
  - Queue rule: "plukk det ELDSTE. Tom kø → heartbeat «ingen kø» på #1110 og avslutt. Det er suksess, ikke tomgang." (step 1)
  - Contract-comment requirement + `autonomy:blocked` outcome + max-one-retry: "Mangler den → sett `autonomy:blocked` + norsk kommentar (...) og plukk NESTE fra køen (maks ett ekstra forsøk per natt — deretter avslutt)." (step 2.1)
  - Green-main preflight that aborts the night: "`npm ci` + gates (...) på fersk main FØR bygging. Rød → avbryt hele natten: verifiser at CI-vakt-varselissue finnes (opprett hvis ikke), heartbeat «avbrutt — main rød», ferdig." (step 2.2)
  - #1077 convergence-rules reference with round-history committing: "**#1077-konvergensreglene gjelder** (docs/forge-workflow.md → Konvergensregler): runde-historikk i `.forge/evaluations/<slug>-runder.md` committes, maks 5 evaluate-runder, strategibytte etter to identiske finding-sett" (step 3)
  - e2e-or-needs-manual-qa outcome: "kjør `npm run e2e:gate`. Grønn → noter i PR-kommentaren." / "sett `needs-manual-qa`-label på PR-en og skriv (...) nøyaktig hvilken flyt som må klikkes gjennom (...) Dette er et eksplisitt utfall — aldri hopp stille over." (step 4)
  - Draft-PR + Closes + autonomy:review + never merge: "**ALDRI merge.** Leveransen er draft-PR-er på `claude/`-brancher. Eieren merger." (hard rule) + "DRAFT-PR med `Closes #<issuenr>` i body, `autonomy:review`-label" (step 5)
  - Heartbeat-always with exact outcome vocabulary: "🌙 Nattkjøreren <dato>: <utfall> der utfall er ett av: `bygde #N → PR #M (review-klar)` / `blokkerte #N (<grunn>)` / `ingen kø` / `avbrutt — main rød` / `avbrutt — <miljøfeil>`." (step 6, titled "ALLTID, uansett utfall")
  - 1-issue budget: "**Budsjett v1: maks 1 issue per natt.** Økes kun av eieren" (step 1)
- [x] **Criterion 2 — morgenbriefen.md required elements.** All present:
  - Delta logic: "finn forrige brief-kommentar på #1110 (nyeste kommentar som starter med `☀️ Morgenbrief`); rapporter kun endringer etter dens tidsstempel."
  - Verify-before-include with unverifiable→loop-failure: "En påstand som ikke lar seg verifisere rapporteres som **loop-feil** i briefen — aldri som suksess."
  - Three-group action-line format: template block with `**Trenger deg nå:**` / `**Skjedde i natt/i går:**` / `**Loop-helse:**`, one action per line with link, e.g. "Godkjenn PR #M — ... → <lenke>".
  - Heartbeat watch, one-missing=warning / two-in-a-row=deduped infra-issue: "**Mangler én kjøring:** varsellinje øverst (...)" / "**Mangler to på rad:** i tillegg opprett infra-issue (...) dedupet mot åpent issue med samme tittel."
  - Empty-night line: "**Tom natt gir én linje** («ingen aktivitet — heartbeats OK»), aldri ingenting."
  - CI-vakta v1 exception explained: "CI-vakta poster ikke heartbeat i v1 (24/døgn er støy) — dens helse måles indirekte: åpne `CI-vakt:`-issues eldre enn 24 t uten aktivitet flagges."
- [x] **Criterion 3 — ci-vakta.md and dok-avstemmeren.md point at #1110, CI-vakta exception documented.**
  - `ci-vakta.md` diff: "Heartbeat: CI-vakta poster IKKE heartbeat på Loop-drift-issuet #1110 i v1 (24 kommentarer/døgn er støy). Liveness sees på claude.ai/code/routines; Morgenbriefen (#1080) flagger i stedet `CI-vakt:`-issues eldre enn 24 t uten aktivitet."
  - `dok-avstemmeren.md` diff adds explicit step 4: "**Heartbeat (ALLTID):** avslutt hver kjøring med én kommentar på det pinnede Loop-drift-issuet **#1110**: `📋 Dok-avstemmeren <dato>: <utfall>`"
- [x] **Criterion 4 — labels + #1110 pin/body.**
  - `gh label list --search autonomy`: `autonomy:ready`, `autonomy:blocked`, `autonomy:review` all exist with descriptions referencing #1079.
  - `gh issue view 1110 --json isPinned,labels,milestone`: `isPinned: true`, milestone #13 "Selvkjørende loops", label `area:devx`.
  - #1110 body matches conventions expected by morgenbriefen.md/nattkjoreren.md: names Nattkjøreren + Dok-avstemmeren heartbeats (incl. empty-run heartbeats), Morgenbriefen at 06:30, "stillhet skal bety feil, aldri «ingenting å melde»", and explicitly states "CI-vakta (...) poster IKKE heartbeat her i v1 (...) — dens liveness sees på claude.ai/code/routines" — this is the same wording/logic as in ci-vakta.md and morgenbriefen.md. Consistent.
- [x] **Cross-consistency (docs/forge-workflow.md, package.json, tests/hooks/guard.test.sh, .forge/templates/eskalering.md).** All commands/paths nattkjoreren.md references actually exist in the repo: `npm run typecheck`/`test`/`lint`/`e2e:gate` are real package.json scripts, `tests/hooks/guard.test.sh` exists, `.forge/templates/eskalering.md` exists and matches the round-history + A/B-question shape nattkjoreren.md step 5 describes, and `docs/forge-workflow.md` "Konvergensregler" section matches what's cited (round-history file, finding-signature, no-progress strategy switch, hard caps, non-convergence-always-has-artifact).
- [x] **Sanity vs. epic #1073 hard rules + CLAUDE.md.** Checked epic body directly: all 5 hard rules ("Aldri prod", "Aldri merge uten menneske", "Sekvensiell branch-disiplin", "Eskaleringer på norsk og handlingsrettede", "Fail-closed") are respected in the new docs — no contradictions found. Docs-only commits correctly use `docs(...)` prefix (bypasses version-bump hook per CLAUDE.md); commit bodies use `Refs #N` as required.
- [~] **Criterion 5 — round-history for this build committed.** No `.forge/evaluations/1079-1080-natt-duo-runder.md` (or similar) exists yet. Per #1077 rules this file is appended "after each evaluate round" — this is round 1 of evaluation for this contract, so its absence going into this evaluation is not itself a violation. It becomes a real gap only if this evaluation records ACCEPT and no round-history file is committed to close the loop (see Findings).
- [ ] **Criterion 6 — first night run / first brief.** Explicitly PENDING FIRST USE per the contract; not evaluable statically. Correctly flagged as such in the contract itself.

## Findings

1. **(Process, not a doc defect) Round-history file for this contract's own build is missing as of this evaluation.** #1077's convergence rules require a `.forge/evaluations/<slug>-runder.md` after each evaluate round, and this contract's own build should follow that rule reflexively. Since this is the first evaluate round and the verdict is ACCEPT, whoever lands this evaluation should append a round-history entry (`1079-1080-natt-duo-runder.md`: round 1, ACCEPT, no findings) and commit it with `Refs #1079` + `Refs #1080` before merge, per the letter of the rule the contract itself asks nattkjoreren to follow. Minor, mechanical, does not block ACCEPT but should not be silently skipped either.

2. **Minor: morgenbriefen.md's monthly-archival step doesn't restate "draft PR, never merge."** Step says "flytt forrige måneds kommentarer til `docs/loops/logg/<år>-<måned>.md` via docs-PR" without repeating the never-merge constraint that nattkjoreren.md states explicitly. Low risk (docs-only archival of the loop's own comment log, not app code), and the epic-level "aldri merge uten menneske" rule already covers it — but worth a one-line tightening ("...via docs-PR (draft, eier merger)") for consistency with how carefully the rest of the pair states outcomes explicitly. Not a blocker.

3. **No hidden interactive-only dependency found.** Both "interaktiv økt" references in nattkjoreren.md are exactly the two designed fallback paths (missing forge-contract → punt to interactive `/forge:contract`; missing staging e2e env → `needs-manual-qa` label + punt to #1076's interactive stagingbevis-port). Nothing else in either doc requires a tool a cloud routine running gh+npm couldn't execute.

4. **Labels, pin, and cross-doc references all check out live against GitHub**, not just against the diff — #1110 is pinned with milestone 13 and body language matching what the three loop docs assume; all three `autonomy:*` labels exist with correct descriptions; #1079/#1080 issues are open and match the contract's issue numbers.

5. **No contradiction with epic #1073's hard rules or CLAUDE.md** found on direct comparison (never-prod, never-merge, sequential branch discipline, Norwegian escalations, fail-closed all honored; docs-only commit convention correctly followed, bypassing version-bump hook as intended).
