# Evaluering: docs-trio 1566/1626/1403 — runde 1

Verdikt: **ACCEPT**

Evaluator: fersk kontekst, branch `claude/docs-trio-1566-1626-1403` @ e191f3f3.
Diff-omfang: `git diff origin/main...HEAD --stat` → 7 filer (3 kontrakter + 4 målfiler),
ingen filer utenfor kontraktene. Ingen `.changes/`-notat (korrekt for docs).

## Kontrakt #1566 — bump-ordlyd

- ✅ **Ingen per-commit-bump som gjeldende praksis-eksempel.**
  `grep -inE 'bump' docs/agent-discipline/procedures/commit-and-pr.md docs/test-discipline.md`
  → 0 treff. Ny ordlyd: commit-and-pr.md steg 5 = «(prefix, tracking reference,
  release-note/changelog artifacts)»; test-discipline.md = «(commit-msg-format, manglende
  `.changes/`-notatfil, versjonsfelt-endring utenfor ukesrutinen)». Sistnevnte matcher det
  commit-msg-hooken faktisk blokkerer i dag (CLAUDE.md §Versjonering).
- ✅ **commit-and-pr.md forblir repo-agnostisk.**
  `grep -inE 'torny|tørny|golf-app|\.changes|weekly-release|ukesrutin' commit-and-pr.md`
  → 0 treff. «Release-note/changelog artifacts» er generisk; repo-spesifikken bor fortsatt
  i bindings §T6 (uendret). `.changes/`-referansen står kun i test-discipline.md, som er
  repo-spesifikk — greit.
- ✅ **Kun ordlyds-endring.** Diffen viser nøyaktig 1 endret linje per fil (2 filer,
  ±1 linje hver); ingen struktur-/heading-endringer.

## Kontrakt #1626 — test:rls stale-volum-kur

- ✅ **Symptom + årsak + kur dokumentert.** Nytt 6-linjers varselavsnitt
  (supabase/tests/README.md:54–59): symptom «første `npm run test:rls`-kjøring masse-rød
  … uten hint om årsaken», årsak «`supabase start` gjenbruker et eksisterende volum uten
  å re-påføre migrasjoner», kur «kjør `supabase db reset` … før `npm run test:rls`».
  Faktainnhold stemmer med Supabase CLI-semantikk (`db reset` re-påfører lokale
  migrasjoner); kontrakten bokførte selv VERIFICATION GAP på kjøring — akseptert som spesifisert.
- ✅ **Plassering ved standardveien (A).** Avsnittet står under «## Slik kjører du» →
  «### A) Standardveien», rett etter kjøre-kodeblokken (linje 46–52), før «### B»-headingen.
- ✅ **Ingen andre README-endringer.** Diffen for README.md er kun det 7-linjers innskuddet
  (+7/−0); #533-statusteksten urørt.

## Kontrakt #1403 — SmartLink-JSDoc

- ✅ **Sesjons-rendret-påstanden er borte.** Gammel tekst «rendered with the user's
  session» / «destination's data is already in the router cache» er fjernet; ny tekst sier
  eksplisitt «Session-dependent holes are NOT prefetched» og «it does not pre-load the
  user's data — don't reach for it as an offline strategy» (SmartLink.tsx:15–21).
- ✅ **Ny tekst dekker (a)+(b)+(c) og er faktisk korrekt mot Next-dokumentasjonen**
  (lest selv, node_modules/next/dist/docs/01-app/02-guides/prefetching.md):
  - (a) «only the prerendered shell (static parts + `loading.js` UI) is fetched into the
    router cache» — matcher tabellen L26–30 («Prefetched: No, unless `loading.js`») og
    PPR-seksjonen L206–212 («The shell, which can be prefetched, streams immediately»).
  - (b) «they take a server roundtrip at navigation and stream into the shell» — matcher
    L30 («Server roundtrip on click: Yes, streamed after shell») og L211 («Uncached data
    streams when ready»).
  - (c) «makes the destination's shell paint immediately on tap» + «on touchstart (mobile)
    and mouseenter (desktop), in addition to Link's own viewport-entry prefetch» — dekker
    hva SmartLink reelt kjøper og at den utfyller viewport-prefetchen. (Kontraktens
    parentes-formulering «for lenker Next ellers ikke rakk/prioriterte» er ikke gjengitt
    ordrett, men substansen — tidligere/i tillegg til viewport — står. Vurdert oppfylt.)
  - `cacheComponents`-premisset verifisert reelt: `next.config.ts:14` har
    `cacheComponents: true`.
- ✅ **Kun kommentaren endret.** Diff-hunken for SmartLink.tsx berører bare JSDoc-blokken
  (alle endrede linjer er `*`-prefiksede kommentarlinjer); komponentkoden er byte-identisk.

## Gates (stikkprøve)

- ✅ Commit-prefikser: `docs:` × 3 + `chore(forge):` for kontraktene; alle med korrekt
  `Refs #N` i body (`git log origin/main..HEAD --format='%h %s%n%b'`).
- ✅ Typegate: `npx tsc --noEmit` → exit 0 (EXPECT: 0 — traff). Full `npm run build` ikke
  re-kjørt av evaluator: øvrige endringer er ren markdown, .tsx-endringen er kommentar-only
  og tsc-grønn.

## Scope-kryp

Ingen. Diffen inneholder utelukkende de fire kontraktfestede målfilene pluss
kontraktfilene selv.
