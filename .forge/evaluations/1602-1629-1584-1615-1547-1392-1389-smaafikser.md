# Evaluering: Sju småfikser — #1602 · #1629 · #1584 · #1615 · #1547 · #1392 · #1389

**Branch:** `claude/smaafikser-1602-1629-1584-1615-1547-1392-1389` (7 commits over `origin/main`, én per issue)
**Kontrakt:** `.forge/contracts/1602-1629-1584-1615-1547-1392-1389-smaafikser.md`
**Evaluator:** fresh-context, skeptisk. Statisk verifisering + staging-klikkrunde (Playwright mot `snwmueecmfqqdurxedxv`).
**Dato:** 2026-08-15

---

## Akseptansekriterier

| # | Kriterium | Verdikt | Bevis |
|---|---|---|---|
| 1 | **#1602** front9-spill aktivt → «Vi sees ved hull 9» / «Hull 6–9 …»; `full` → uendret | **PASS** | Staging-render av front9-spill: `🤫 Vi sees ved hull 9.` + `Hull 6–9 vises når arrangøren avslutter spillet.` Full-spill: `🤫 Vi sees ved hull 18.` + `Hull 10–18 vises …` (byte-identisk med før). Regnestykket verifisert mot `lib/games/holeScope.ts:67` — `firstHalfHoleNumbersForSegment('front9') = [1..5]` (`Math.ceil(9/2)`), så skjult = `[6..9]`; back9 → `[15..18]`, `last = 18`. `holeSegment` kommer fra `game.hole_segment` (`…/holes/page.tsx:208`). |
| 2 | **#1629** `revalidateTag('game-<id>','max')` for hvert spill der CH faktisk ble skrevet; testen låser det | **PASS** | `lib/games/recomputeCourseHandicap.ts:293–297` — kallet ligger inne i `if (affected?.length)`, altså 0-raders skriving revaliderer ikke (AGENTS-felle 2 respektert). **Mutasjonsprobe:** fjernet linja → 4 tester rødt, bl.a. `expected "vi.fn()" to be called with arguments: [ 'game-g-1', 'max' ]`. Fila gjenopprettet med `git checkout --` (`git status` ren). |
| 3 | **#1584** begge stepper-knappene har `tap-extend` med `-4px`; synlig boks uendret | **PASS** | Staging (`/games/<finished>/putter`, «5+» klikket): 2 elementer med `.tap-extend`, hver `getBoundingClientRect` = **36×36**, `--tap-extend` = `-4px`, `::after` `inset: -4px` og boks **44px×44px**. Ingen overlapp: `hitAreasOverlap: false`, 28px mellom de to trefflatene. |
| 4 | **#1615** flisen og `/admin/spillere` teller likt | **PASS** | Staging: Klubbhus-flisen «37 registrert · **1 venter**», `/admin/spillere`-headeren «34 registrert · **1 venter**». SQL-orakel: `invitations?accepted_at=is.null` → **1** rad. Den ene raden har `expires_at = 2026-07-17` (**utløpt**) — altså nøyaktig raden gamle koden ekskluderte. Alle tre kildene (`TilesGrid.tsx:44`, `spillere/page.tsx:39`, `PendingInvitations.tsx:56`) bruker nå identisk kjede `.is('accepted_at', null)`. |
| 5 | **#1547** roster-løs `tournament_participants`-deltaker får se klubb-cup-siden; ny Type A-test grønn | **PASS** | Staging med **negativ kontroll**: seedet klubb-scopet utkast-cup (`group_id` = «Tørny»), spiller verifisert `is_admin: false` og `group_members`-treff = 0. Uten deltaker-rad → `[data-testid=not-found]` (= oppførselen før fiksen). Etter `INSERT tournament_participants` → siden rendrer, `h1 = «E2E-1547-klubbcup»`. Eneste endrede variabel er deltaker-raden. Kolonnenavn bekreftet i `lib/database.types.ts:1686–1691` (`tournament_id`, `user_id`); RLS `tournament_participants_select_authenticated using (true)` i `supabase/migrations/0155_…:62–66`. Begge call-sites sender `tournamentId` (`cup/[id]/page.tsx:47`, `cup/[id]/resultater/page.tsx:50`). Ny test `lib/cup/cupPageAccess.test.ts` dekker de tre casene med riktig early-return-rekkefølge (roster først, `getServerClient` aldri kalt). |
| 6 | **#1392** DB-feil i innboksen → error-grense, ikke tomtilstand | **PASS** (m/ gap) | `app/[locale]/innboks/page.tsx:42` og `:75` — begge spørringene destrukturerer `error` og kaster. `app/[locale]/error.tsx` finnes. Normalvei verifisert på staging: `/no/innboks` rendrer 4 varsel-rader, **0 konsollfeil**. Kast-veien kan ikke tvinges fram på staging uten å ødelegge databasen — se VERIFISERINGSGAP. |
| 7 | **#1389** aria-label på innboks-fanen bærer uleste-tall; test grønn | **PASS** | Staging: `aria-label = «Innboks, 12 uleste»`, SQL-orakel `notifications?read_at=is.null&archived_at=is.null` → **12**. Prikken `[data-testid=bottomnav-innboks-dot]` finnes, `background-color: rgb(184, 148, 70)` = `#b89446` = `--accent-deep` (`app/globals.css:41`). ICU-plural-nøkkelen `nav.inboxUnread` finnes i **begge** locales. `BottomNav.test.tsx` asserter tallet i labelen. **0 MISSING_MESSAGE.** |
| 8 | Alle `.changes/`-notater parser | **PASS** | `node scripts/weekly-release.mjs --dry-run` — alle 7 notatene folder inn i `1.233.0` med korrekt `fix`-plassering. |
| 9 | `npm run build` · `npm run lint` · co-located vitest grønt | **PASS** | Se Porter. |

---

## Porter

| Port | Kommando | Resultat |
|---|---|---|
| Unit | `npx vitest run lib/games/recomputeCourseHandicap.test.ts lib/cup components/ui components/hole app/[locale]/innboks app/[locale]/admin app/[locale]/games/[id]/leaderboard` | **154 filer / 1395 tester grønt** |
| Typer | `npx tsc --noEmit -p .` | **exit 0** |
| Build | `npm run build` | **exit 0** |
| Lint | `npx eslint` på alle 14 endrede kildefiler | **0 errors**, 1 warning (`notFound` ubrukt i `drilldown.tsx:3`) — **pre-eksisterende**, finnes identisk på `origin/main` |
| Changelog | `node scripts/weekly-release.mjs --dry-run` | **grønt**, ingenting skrevet |
| Mutasjon | fjernet `revalidateTag` i `recomputeCourseHandicap.ts` | **4 tester rødt** → oppførselen er reelt låst |

---

## Staging-bevis

Alt kjørt mot `torny-staging` (`snwmueecmfqqdurxedxv`) via `next dev` på port 3143 startet fra denne worktreet (`lsof -d cwd` bekreftet), Playwright-drevet, OTP mintet med service-role.

| Akseptansepunkt | Struktur-orakel | Feillogg | SQL-orakel |
|---|---|---|---|
| 1 · #1392 innboks rendrer | `[data-testid=bottom-nav]` + 4 `li` i `main`, ekte varselrader | 0 konsollfeil, 0 requestfailed | — |
| 2 · #1389 bunn-nav uleste | `[data-testid=bottomnav-innboks-dot]` finnes; `aria-label = «Innboks, 12 uleste»`; prikk `rgb(184,148,70)` = `--accent-deep` | 0 konsollfeil, **0 MISSING_MESSAGE** | `notifications` uleste for admin = **12** ✅ matcher labelen |
| 3 · #1615 invitasjonstelling | Flis-lenke `a[href$="/admin/spillere"]` = «37 registrert · 1 venter»; header = «34 registrert · 1 venter» | 0 konsollfeil | `invitations?accepted_at=is.null` = **1** ✅ lik begge steder (og raden er utløpt — den gamle koden hadde vist 0 i flisen) |
| 4 · #1602 skjult-kort m/ segment | Kort-container `.border-dashed` rendret i drilldownen; front9: «Vi sees ved hull 9» / «Hull 6–9 …», full: «hull 18» / «Hull 10–18 …»; tabellen viser kun hull 1–5 for front9 | 0 konsollfeil, **0 MISSING_MESSAGE** | `games.hole_segment` = `front9` hhv. `full` |
| 5 · #1584 putte-stepper | 2 × `.tap-extend`, boks 36×36, `--tap-extend: -4px`, `::after` 44×44 (`inset: -4px`), overlapp = `false`, 28px klaring | 0 konsollfeil | — |
| 6 · #1547 cup-gate | **Negativ kontroll:** uten deltaker-rad → `[data-testid=not-found]`. **Etter innsetting:** `h1 = «E2E-1547-klubbcup»`, ingen not-found | 0 konsollfeil | Spiller `is_admin=false`, `group_members`-treff = **0**, `tournament_participants` = **1** rad (eneste endrede variabel) |
| 7 · #1629 CH-cache | Ikke UI-drevet — se VERIFISERINGSGAP (enhetstest + mutasjonsprobe dekker) | — | — |

**Prod-vakt: alle Supabase-kall mot staging-ref ✅ (15 kall totalt over to økter — `snwmueecmfqqdurxedxv.supabase.co`, 0 prod-treff).**

**Opprydding:**
- Seedet `E2E-1547-klubbcup` + deltaker-rad **slettet** (204/204); `tournaments?name=like.E2E-*` → `[]`, `tournament_participants` for cupen → `[]`.
- Front9-spillets `score_visibility` midlertidig `reveal` → `live` for å nå drilldownen, **gjenopprettet til `reveal`** og verifisert med REST etterpå.
- `invitations` uendret (fortsatt 1 ventende).
- Dev-serveren stoppet. `git status` ren bortsett fra den usporede kontraktsfila under `.forge/`.

---

## Findings

| # | Signatur | Alvorlighet | Beskrivelse |
|---|---|---|---|
| F1 | `app/[locale]/admin/TilesGrid.tsx` + `app/[locale]/admin/spillere/page.tsx` — kriterium 4 (#1615), **utenfor kontraktscope** | **Lav — ny issue anbefales** | Nabotallet har nøyaktig samme divergens som #1615 fikset: flisen teller **alle** `users`-rader (37 på staging), mens `/admin/spillere` teller kun `profile_completed_at NOT NULL AND deleted_at IS NULL` (34). Samme to flater, samme «én regel, ett hjem»-brudd, ett felt til venstre. #1615 nevnte kun invitasjonene, så dette hører ikke hjemme i denne PR-en — men bør files som eget issue før merge (reviewer-funn-regelen). |
| F2 | `lib/games/recomputeCourseHandicap.test.ts:463` — kriterium 2 (#1629) | **Lav — nit** | Testtittelen “hånd-redigert overstyring: **ingen skriving, ingen revalidering**” asserterer nå `toHaveBeenCalledTimes(1)`. Byggerens begrunnelse er **verifisert riktig** (mutasjonsproben viser at casen faktisk skriver CH, bare ikke `mode_config`), og en kommentar forklarer det — men tittelen er det CI-loggen viser, og den sier nå det motsatte av assertet. Foreslått: “…: ingen mode_config-skriving, kun CH-revalidering”. |
| F3 | `components/ui/BottomNav.tsx:112` — kriterium 7 (#1389) | **Info** | `dot ? t('inboxUnread', { count }) : label` i stedet for kontraktens `dot && hasUnread`. **Ikke en defekt i dag**: `dot: hasUnread` for innboks-fanen og `dot: false` for de tre andre, så uttrykkene er ekvivalente (verifisert i koden og på staging — de tre andre fanene har labels «Hjem»/«Klubbhuset»/«Profil»). Koblingen er implisitt: gir noen en annen fane `dot: true` senere, arver den innboks-labelen stille. |

Ingen av funnene er defekter mot kontrakten eller issue-intensjonen, og ingen bryter test-disiplinen (ingen nye Type C-tester, ingen kopiert mock-oppsett, ingen “mens jeg var her”-tester — de tre nye testene svarer hver på sitt eget spørsmål).

---

## Verifiseringsgap

1. **#1392 kast-veien.** Kan ikke tvinges fram på staging uten å ødelegge databasen (droppe tabell / bryte RLS). Dekket av kodelesing (begge spørringene kaster) + at `app/[locale]/error.tsx` finnes. Normalveien er drevet live med 0 konsollfeil.
2. **#1629 ingen UI-drive.** Dekket av enhetstest + mutasjonsprobe (fjernet kall → 4 tester rødt) og av at mønsteret er identisk med det allerede shippede greensome-passet på `:410` i samme fil.
3. **#1602 front9 krevde en midlertidig dataendring.** Det eneste aktive front9-spillet på staging har `score_visibility='reveal'`, og `reveal-active` returnerer `RevealHiddenView` før drilldownen i det hele tatt nås. Jeg flippet feltet til `live`, drev ruta, og gjenopprettet `reveal` (verifisert). Merk: `game-${id}`-cachen serverte fortsatt `live` under proben — REST-skriving invaliderer den ikke (kjent staging-felle).
4. **#1584 kun `/putter`-flaten.** Verifisert på putte-etterfyllingen (ferdig spill). Submit-prompten (`PuttsSubmitPrompt`) monterer nøyaktig samme komponent med samme props og er ikke drevet separat.

---

## VERDICT: ACCEPT

Alle ni akseptansekriterier oppfylt med bevis, alle porter grønne, sju uavhengige fikser holdt rene fra hverandre (én commit per issue). To av fiksene har diskriminerende orakler som ville falt på gammel kode: **#1615** (den ene ventende invitasjonen er utløpt — gammel flis-kode hadde vist 0) og **#1547** (negativ kontroll gir 404, deltaker-raden alene åpner siden). Byggerens sju avvik er verifisert og velbegrunnet.

Anbefaling før merge: file **F1** som eget issue (med milestone), jf. reviewer-funn-regelen. F2/F3 kan tas som en linjes opprydding neste gang filene røres.
