# Evaluering: fire UI-/a11y-funn (#1387 #1673 #1388 #1393)

**Branch:** `claude/batch-a-1387-1673-1388-1393` @ `eb5a44af` (7 commits over `origin/main`: kontrakt + 5 kode + 1 guard)
**Kontrakt:** `.forge/contracts/1387-1673-1388-1393-ui-a11y.md`
**Evaluator:** fersk kontekst, statisk + unit + mutasjonsprober + staging-klikkrunde. Ingen produkt-/testfiler endret permanent.

## Success criteria

| # | Kriterium | Verdict | Bevis |
|---|---|---|---|
| 1 | **#1387** `role="button"` borte fra ScoreCard; suitene grønne med testid-håndtak; staging: kortet vises, +/− virker, kortet har ikke `role` | PASS | `grep -n 'role="button"' components/hole/ScoreCard.tsx` → **0 treff** (exit 1). `components/hole/ScoreCard.tsx:290` er nå `<div data-testid="score-card" style={cardStyle} onClick={onCardClick}>` med en 7-linjers kommentar over som begrunner fjerningen (presentational children, #1387). Testene bruker håndtaket: `ScoreCard.test.tsx:29` `getByTestId('score-card')`, `HoleClient.test.tsx:337` `getAllByTestId('score-card')`. Ny kollaps-vern-assert `ScoreCard.test.tsx:77-82`. **Mutasjonsprobe:** re-innsatt `role="button"` på div-en → `ScoreCard.test.tsx` **1 failed / 44 passed** (den nye asserten faller på `card.getAttribute('role')` = `"button"`); reverted med `git checkout --`. Staging (aktivt spill, admin): `[data-testid=score-card]` × **2**, begge med `role=null` **og** `aria-label=null`; `+` på hull 1 (par 5) tomt kort → **6**; hull 2 (par 3): tomt → `+` → **4** → `−` → **3**, og `scores`-raden i staging-DB fikk `strokes: 3`. Tab-vandring landet på den nestede `+1`-knappen med `:focus-visible` = true — nettopp det rollen blokkerte. i18n-nøkkelen `setScoreAriaLabel` er fjernet symmetrisk fra `messages/no.json` og `messages/en.json` (0 gjenværende treff i repoet utenom kontrakt-dokumentet); `grep -rn "Sett score for" e2e/` = 0. |
| 2 | **#1673** valgt flis' knapp har utvidet negativ outline-offset; staging-skjermbilde (Tab, steg 2) viser ring adskilt fra kanten i lys OG mørk modus | PASS | `FormatGrid.tsx:95` `style={{ outlineOffset: '-5px' }}` på `<button>` i `if (selected)`-grenen, med 13 linjers begrunnelse over. Staging, veiviser steg 2, Stableford valgt, tastatur-fokus (Shift+Tab→Tab, ikke programmatisk `.focus()`): `focusVisible: true`, `outlineOffset: "-5px"`, `outlineWidth: "2px"`, `outlineStyle: "solid"`, `outlineColor: rgb(27,67,50)` lys / `rgb(212,184,112)` mørk. Wrapper: `data-focus-inset` = true, `bg rgb(232,239,232)` (= `--primary-soft`) lys / `rgb(31,44,36)` mørk, border `rgb(27,67,50)` lys. **Mutasjonsprobe (live, i nettleseren):** fjernet inline-stilen → `outlineOffset` faller til **`-2px`** — altså er det inline-stilen som produserer −5px, og globals-regelen som gir −2px. Skjermbilder lys + mørk, pluss kontroll-skjermbilder i pre-fiks-tilstand (−2px). Se «Skjermbilder» under. |
| 3 | **#1388** `--warning-text` i :root + begge dark-blokker + @theme; `grep -rn "text-warning\b" app components lib \| grep -v text-warning-text` = **0**; kontrast-testen grønn; staging viser computed color `#7a5410` i lys modus | PASS | `app/globals.css:67` `--warning-text: #7a5410` (:root), `:190` (media-dark) og `:287` (`[data-theme='klubbhus-natt']`) begge `#e5b26f`, `:349` `--color-warning-text: var(--warning-text)` i `@theme`. Sweepen: **0** (bekreftet med to uavhengige regex-former, `\b` og `text-warning([^-]|$)`); 31 `text-warning-text`-treff i 22 filer. `--warning` selv er urørt (`#d89b3a`) og `bg-warning/*` + `border-warning/*` står igjen. Ny Type A-lås `app/__tests__/warning-text-contrast.test.ts` (11 tester) på de delte leserne. **Mutasjonsprobe:** satte `--warning-text` tilbake til `#d89b3a` → **4 failed / 7 passed** (alle fire lys-modus-kontrastene, verste måling 1,96:1 mot amber-tinten); reverted. Staging, `/no/admin/games/[id]`, «Avslutt spillet»-boksen: computed `color` = **`rgb(122, 84, 16)`** = `#7a5410` ✓ på `bg-warning/10`. Mørk modus lastet natively (`colorScheme: 'dark'` fra første paint): `--warning-text` = `#e5b26f`, computed `color` = `rgb(229,178,111)`, og hele den malte flate-kjeden bak teksten er mørk (`rgb(28,42,34)` kort → `rgb(26,38,32)` admin-skall → `rgb(20,32,26)` body). |
| 4 | **#1393** partisjons-helper + Type A-tester; page.tsx arkiverer stale i `after()`; staging: rigget stale-varsel får `archived_at`+`read_at` og ulest-tellingen faller | PASS | `partitionStaleSignupNotifications` (`staleNotifications.ts:43`) returnerer `{visible, stale}`; `filterStaleSignupNotifications` er **helt borte** — `grep -rn` over `app components lib e2e hooks scripts` gir **0 dinglende kallesteder** (kontrakten tillot «behold som wrapper ELLER bytt call-siten»; call-siten ble byttet). 5 Type A-cases i `staleNotifications.test.ts:47-108` (tom, kun stale, blandet m/ rekkefølge i begge bøtter, andre kinder alltid visible). `innboks/page.tsx:82-87` kaller `after(() => archiveStaleNotifications({userId, ids}))`; helperen (`lib/notifications/archive.ts:117-137`) bruker admin-klienten, er scopet `.eq('user_id', userId).in('id', ids).is('archived_at', null)`, setter `archived_at` + `read_at` i én skriving, logger `console.error('[innboks] archive stale failed', …)` og kaster aldri, og speiler `markRead.ts:81`s `revalidateTag(\`notifications-${userId}\`, 'max')`. Doc-kommentaren i `staleNotifications.ts:26-34` er oppdatert. **Staging (ekte data + rigg):** admin hadde 4 uleste, hvorav **3 var ekte stale påmeldings-varsler for slettede spill** — nøyaktig fantom-prikken issuet beskriver. Rigget i tillegg kontraktens rad (`game_id 00000000-…00ff`, «Slettet turnering») **og** en kontroll-rad som peker på et spill som finnes. Etter ett besøk på `/no/innboks`: rigget stale-rad `read_at` = `archived_at` = `2026-08-16T09:27:11.879+00:00`; **kontroll-raden urørt** (`read_at: null`, `archived_at: null`) og fortsatt synlig i lista («Kontroll finnes» rendret, «Slettet turnering» ikke); uleste **6 → 2**; bunn-nav-en leste `aria-label="Innboks, 2 uleste"`. De 2 som står igjen er kontroll-raden + et `scorecard_submitted` — begge legitimt uleste, altså ble ingen ikke-signup-rad rørt. Ingen `[innboks] archive stale failed` i server-loggen. |
| 5 | `.changes`-notater parser; build/lint/co-located vitest grønt | PASS | Se Gates. Alle fire notatene har gyldig frontmatter (`type: fix` + `issue`, ingen `title`/`link`/`cta` — riktig for `fix`), brødtekst på idiomatisk norsk i du-form, godt under 400 tegn. Dry-run folder dem inn i `1.233.0`. |

## Gates

| Gate | Kommando | Resultat |
|---|---|---|
| Unit (kontraktens sett) | `npx vitest run components/hole "app/[locale]/games/[id]/holes" "app/[locale]/admin/games/new/FormatGrid.test.tsx" app/__tests__ lib/notifications "app/[locale]/innboks"` | **36 filer / 391 tester grønne** |
| Typecheck | `npx tsc --noEmit -p .` | **exit 0**, 0 linjer output |
| Build | `npm run build` (`set -o pipefail`) | **exit 0** |
| Lint | `npm run lint` (`eslint`, hele prosjektet) | **exit 0 — 0 errors**, 56 pre-eksisterende warnings (complexity + to unused-vars i `drilldown.tsx`/`page.tsx`, alle fra før; ingen ligger på en linje denne branchen rører) |
| Ukesslipp | `node scripts/weekly-release.mjs --dry-run` | **exit 0**; alle fire notatene folder inn i `1.233.0` |
| Snapshots | `grep -rl "text-warning" **/*.snap` | **0 treff** — ingen snapshot låser klassestrengen, så `vitest -u` var ikke nødvendig (kontrakten forutså det som mulig) |
| Mutasjonsprobe #1387 | `role="button"` re-innsatt på kort-div-en | 1 failed / 44 passed → **asserten er ekte** |
| Mutasjonsprobe #1388 | `--warning-text` → `#d89b3a` | 4 failed / 7 passed → **kontrastlåsen er ekte** |
| Mutasjonsprobe #1673 | inline `outline-offset` strippet live i Chromium | −5px → **−2px** → **inline-stilen er det som virker; globals gir −2px** |

## Staging-bevis

Kjørt mot `torny-staging` (`snwmueecmfqqdurxedxv`) fra denne worktreen: `rm -rf .next` + `next dev -p 3145` bootet via node-wrapper med `.env.staging.local` i prosess-env, cwd verifisert med `lsof -a -p <pid> -d cwd` = `…/worktrees/forge-auto-issue-0715fa`. Innlogging via service-role `generate_link` → `email_otp` (POST /login → 303). Playwright via Bash, mobil-viewport 430×950.

| Akseptansepunkt | Struktur-orakel | Feillogg | SQL-orakel |
|---|---|---|---|
| **#1387** — kortet er ikke lenger én stor knapp | 2 × `[data-testid=score-card]`, begge `role=null` + `aria-label=null`. Fire ekte knapper i kortet med scoret verdi (`Nullstill scoren for …`, `+1`, `-1`, `Velg spesifikk score`), tre før første score. Tab lander på `+1` med `:focus-visible`. | 0 console-errors, 0 MISSING_MESSAGE, 1 `requestfailed` = benign `ERR_ABORTED` på login-navigasjonen | Hull 2: tomt → `+` → `−` skrev `scores.strokes = 3` for admin. Raden slettet etterpå; spillet står igjen med **0** scores, som før. |
| **#1673** — fokusringen er synlig på valgt flis | Valgt Stableford-flis, tastaturfokus: `outline 2px solid`, `outline-offset -5px`, ring `#1b4332` (lys) / `#d4b870` (mørk) mot flisens `--primary-soft`-fyll. Strippet inline-stil → `-2px`. | 0 console-errors, 0 MISSING_MESSAGE | — (ren presentasjonsendring) |
| **#1388** — advarselstekst er lesbar | `/no/admin/games/[id]`, «Avslutt spillet»-boksen (`bg-warning/10 border-warning/30 text-warning-text`): computed `color` `rgb(122,84,16)` lys, `rgb(229,178,111)` mørk. Malt flate-kjede i mørk modus verifisert mørk hele veien opp til `body`. | 0 console-errors, 0 MISSING_MESSAGE | — (ren token-endring) |
| **#1393** — fantom-prikken slukner | `/no/innboks`: «Slettet turnering» **ikke** i lista, «Kontroll finnes» **er** i lista. Bunn-nav `aria-label` gikk fra 6 til «Innboks, 2 uleste». | 0 console-errors, 0 MISSING_MESSAGE, `ERR_ABORTED` kun på unread-pollen (benign) | Rigget stale-rad: `read_at` + `archived_at` begge satt. Kontroll-rad: begge fortsatt `null`. Uleste 6 → 2. Ingen `archive stale failed` i serverloggen. |

**Prod-vakt: 0 brudd — alle Supabase-kall i alle fem drivings-øktene gikk til `snwmueecmfqqdurxedxv.supabase.co`.** Ingen forespørsel traff prod-ref-en `glofubopddkjhymcbaph`, til tross for at worktreens `.env.local` peker på prod (staging-env-en i prosess-env vinner, som forutsatt).

### Skjermbilder

| Fil | Viser |
|---|---|
| `…/scratchpad/1673-selected-focus-light.png` | Valgt Stableford-flis, lys modus, tastaturfokus: mørkegrønn ring **inne i** flisen med et synlig lysegrønt gap ut til flisens egen kant. |
| `…/scratchpad/1673-control-before-light.png` | Samme flis med inline-stilen strippet (pre-fiks, −2px): ringen smelter sammen med border + inset-linje til én tykk grønn kant — ingen ring å se. Dette er defekten issuet beskriver. |
| `…/scratchpad/1673-selected-focus-dark.png` | Mørk modus: gullring inne i flisen, klart adskilt fra den grønne kanten, mot mørkegrønt fyll. |
| `…/scratchpad/1673-control-before-dark.png` | Mørk modus, −2px: ringen er **allerede** synlig her (gull mot grønn kant), jf. funn F2. |
| `…/scratchpad/1388-warning-light.png` | «2 av 2 spillere har ikke levert …» i mørk brun-gull på amber-tint — lesbar, ikke den gamle bleke amberen. |
| `…/scratchpad/1387-hole-page.png` | Hull-siden med to spillerkort, `+`/`−`/`⋯` som egne knapper og «Angre» på kortet med score. |
| `…/scratchpad/1393-innboks.png` | Innboksen etter arkiveringen: kun kontroll-raden og scorekort-varselet. |

### Opprydding

- Begge kode-mutasjonsprobene tilbakestilt med `git checkout --` (`components/hole/ScoreCard.tsx`, `app/globals.css`). #1673-proben var kun i nettleser-DOM-en, aldri på disk.
- Riggede varsler slettet (2 rader). De **tre pre-eksisterende** stale-radene ble arkivert av selve fiksen under klikkrunden — de er tilbakestilt til som-funnet (`read_at: null`, `archived_at: null`) så staging står uendret; et nytt innboks-besøk vil arkivere dem igjen, som er meningen.
- Begge probe-scorene slettet; `scores` for probe-spillet = **0**, admin sine uleste aktive varsler = **4** — begge identisk med baseline før økten.
- Dev-serveren er stoppet (port 3145 ledig).
- `git status` rent bortsett fra `?? .forge/contracts/1678-…md` (Batch B-kontrakten) og denne evalueringsfila.

## Findings

| # | Fil + kriterium | Alvorlighet | Funn | Foreslått handling |
|---|---|---|---|---|
| F1 | `app/[locale]/admin/games/new/FormatGrid.tsx:95` (#1673) | Info (avvik, verifisert) | Inline `style` i stedet for Tailwind-utility. Byggerens begrunnelse holder: globals-fokusreglene ligger bevisst utenfor alle `@layer` (`globals.css:392-398`), og lag-rekkefølge slår spesifisitet, så en `focus-visible:[outline-offset:…]` i `@layer utilities` taper. Målt live: utility-veien gir −2px, inline gir −5px. Sideeffekt: offseten står ubetinget (ikke bak `focus-visible:`), men er inert uten tegnet outline — og globals-regelen tegner kun på `:focus-visible`. | Ingen. Kommentaren i koden forklarer valget for neste leser. |
| F2 | `FormatGrid.tsx` (#1673) | Info (omfang) | Kontroll-skjermbildet i mørk modus viser at ringen var **synlig allerede på −2px** der (gull ring mot grønn kant). Defekten var altså lys-modus-spesifikk; −5px er en forbedring i begge, men ikke en fiks av noe ødelagt i mørk modus. Ingen regresjon. | Ingen — nevnt for sporbarhet i #1673s closing-kommentar. |
| F3 | `.forge/contracts/…:64` (#1388) | Info (kontrakt-drift) | Kontrakten oppga «34 treff i 22 filer»; `origin/main` hadde **31 treff i 22 filer**. Alle 31 er konvertert, så tallet påvirker ingenting — men det er nok en påminnelse om å telle mot HEAD, jf. [[feedback_verify_contract_claims_against_head]]. | Ingen. |
| F4 | `app/[locale]/innboks/page.tsx:42` (#1393) | Lav (kant-tilfelle) | Sida henter `.limit(100)` aktive varsler. Har en bruker mer enn 100 aktive rader, faller stale-rader utenfor vinduet og arkiveres aldri — prikken kan i teorien fortsatt henge. Pre-eksisterende paginering, ikke innført her, og urealistisk på dagens skala. | Ingen nå. Nevn i closing-kommentaren; egen issue kun hvis noen faktisk bikker 100. |
| F5 | `app/[locale]/admin/games/[id]/page.tsx` (utenfor scope) | Info (UX, pre-eksisterende) | «Avslutt spillet»-seksjonen viser **«Se spillerstatus og send påminnelse →» to ganger** — én gang som omrammet knapp over «Avslutt likevel», én gang som ren lenke under. Fanget på skjermbildet under #1388-verifiseringen. Rører ikke denne PR-en. | Egen issue (`type: fix`, `area: admin`, milestone etter flyt-kompasset). |
| F6 | commit `eb5a44af` (#1393) | Info (prosess) | Guard-commiten er en `fix(`-commit med `[no-changelog]` selv om den endrer oppførsel på en feilsti (kaster nå i stedet for å svelge). Forsvarlig — #1393 har allerede et notat som dekker endringen som helhet — men det er strengt tatt ikke «intern» i konvensjonens forstand. | Ingen. |

Ingen av funnene endrer verdikten.

## Kontrakt-avvik (bekreftet, alle akseptable)

1. **#1673 implementasjonsform:** kontrakten foreskrev `focus-visible:[outline-offset:-5px]` som Tailwind arbitrary property; bygget bruker inline `style`. Empirisk verifisert nødvendig (F1) — utility-veien måler −2px i Chromium.
2. **#1673 ingen ny unit-assert:** kontraktens betingelse var «kun hvis testen alt asserter klasser». `FormatGrid.test.tsx` har én Type C-test som kun asserter roller og `onChange`, ingen klassestrenger — betingelsen slår ikke inn, og kriteriet låses av staging-målingen + skjermbildene i stedet. Verifisert, ikke antatt.
3. **#1393 helper-form:** kontrakten tillot å beholde `filterStaleSignupNotifications` som tynn wrapper **eller** bytte call-siten. Byggeren fjernet den helt. Verifisert 0 dinglende kallesteder i `app components lib e2e hooks scripts`.
4. **#1388 dark-dekning i testen:** kontrakten ba om ≥4,5:1 mot dark `--bg` «i begge dark-blokkene». Testen måler kontrast på `DARK_ATTR` og beviser separat at `DARK_MEDIA` har identisk verdi (`warning-text-contrast.test.ts:47-51`). Logisk ekvivalent dekning.
5. **#1388 snapshot-oppfrisking:** kontrakten forutså at Banner/Input/SyncBanner/clubStatus-snapshots kunne låse klassestrengen. Ingen gjør det (0 `.snap`-treff på `text-warning`), så `vitest -u` var ikke nødvendig — commit-bodyen sier det samme.
6. **Ekstra sjette commit (`eb5a44af`), ikke i kontrakten:** eksistens-oppslaget i `page.tsx` kastet ikke ved feil, og et tomt `existing` ville stemplet **alle** påmeldings-varsler som stale — harmløst da stale bare betydde «skjult», men fra #1393 betyr det «arkivert». Guarden er dermed en direkte konsekvens av #1393 og hører hjemme i PR-en. Verifisert at normalstien er uberørt: `/no/innboks` renderte 200 med signup-varsler og fant de eksisterende spillene; `app/[locale]/error.tsx` finnes som feilgrense for ruta.
7. **`.changes`-tekst for #1388** avviker minimalt fra kontraktens utkast (norske hermetegn rundt «venter på godkjenning»/«husk å levere»). Forbedring, ikke avvik i innhold.

## VERIFICATION GAPs

1. **Feilgrenen i `archiveStaleNotifications`** (`console.error('[innboks] archive stale failed', …)` → `return false`) ble aldri utløst live — best-effort by design, og ingen naturlig måte å fremtvinge en PostgREST-feil på uten å tukle med staging-skjemaet. Dekket av lesing av koden: feilen fanges, logges og kastes ikke.
2. **`[data-theme='klubbhus-natt']`-stien for `--warning-text`** ble ikke drevet i nettleseren; mørk modus ble verifisert via `prefers-color-scheme: dark` (media-blokka). Type A-testen låser at de to blokkene har identisk verdi, så stien er dekket av unit, ikke av klikk.
3. **Full-viewport-skjermbilder på `/admin/games/[id]` i mørk modus er upålitelige** — captura viste lyse kortflater selv om hele den malte kjeden computed mørk (`rgb(28,42,34)` / `rgb(26,38,32)` / `rgb(20,32,26)`) og login-sida rendret korrekt mørk i samme kontekst. Konklusjonen om mørk modus hviler derfor på computed-style-målingene, ikke på det bildet. Ikke et branch-problem: diffen rører hverken `--bg` eller `--surface`.

---

**Verdict: ACCEPT**

Ingen blokkerende funn.
