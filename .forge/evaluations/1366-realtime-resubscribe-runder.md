# Konvergensrunder — #1366 realtime resubscribe

Natt 2026-08-14, nattkjøreren (#1079). Bygger: Opus-subagent. Evaluator: Opus-subagent (fersk kontekst). Kryss-modell-gate: Sonnet (Steg 4.5).

| Runde | Utfall | Funn |
|---|---|---|
| 1 | REJECT | Evaluator (med kilde-probe mot pinned realtime-js): **B1** — å fjerne `setAuth` gjør at FØRSTE kanal per sidelast joiner uten access_token (join-payload snapshotes synkront før async token-oppslag rekker frem); verifisert remedie: no-arg `await realtime.setAuth()` før subscribe (primer token, `_manuallySetToken` forblir false). **B2** — ny CLAUDE.md/README/bindings-tekst var faktisk feil (TOKEN_REFRESHED-propagering virker uavhengig av manuelt token; heartbeat-upkeep bruker alltid accessToken-callbacken). **M3** — `consecutiveFailures` nullstilles ved schedule, ikke rebuild → gammel kanal forgifter ny kanals budsjett. **M4** — `parkedUntilOnline` ryddes ikke av `SUBSCRIBED` → frisk kanal rebuildes på neste online-event. **M5** — `docs/uat-empty-states-...md` bærer fortsatt gammel regel. Nits: død `setAuthSpy`-mock, ingen pre-warm-assertion. Verifisert rent: signatur/8 call-sites, new-before-old-ordering, CLOSED-håndtering, unmount-lekkasje, backoff-cap, hygiene. |
| 2 | ACCEPT (m/ minors) | Fiks-commit `742d73e` verifisert i fersk kontekst mot pinned-kildene: B1-remediet korrekt (no-arg `setAuth()` i `openChannel`, dekker initial + rebuild; `getSession`-forvarming droppet som eksakt duplikat), B2-teksten nå kilde-riktig i alle levende hjem, M3 fikset med reset-ved-rebuild + kanal-identitetssjekk, M4 fikset. Gates grønne (full suite 5940). Tre nye minors i den nye async-stien: stale rebuild-timer kan rive frisk kanal (await-vindu), `void openChannel()` uten `.catch` gir terminal død tilstand ved rejection, dok manglet klausul om at supabase-js selv flipper `_manuallySetToken` ved TOKEN_REFRESHED. To nits (identitetssjekk utestet, unreachable blokk). |
| 3 | ACCEPT | Polish-commit `3bea95e` (Sonnet, presise instrukser): timer-clearing øverst i `openChannel`, `.catch` → `scheduleRebuild` på begge fire-and-forget-stier, dok-klausul i CLAUDE.md + JSDoc, identitetssjekk-test bevist fail-without-fix (topic `#2` vs `#3`), unreachable blokk fjernet. Full suite 5940 grønn, lint 0 feil, typecheck ren. → Steg 4.5. |
| 4 | REJECT (kryss-modell-gate, Sonnet) | Empirisk reprodusert race: orphan retry-timer kan armeres MENS `openChannel` står suspendert på `await setAuth()` (gammel kanal fortsatt `channelRef`, `retryTimer` null etter at timeren nullet seg selv) — timeren overlever swappen og river den friske erstatningskanalen senere (`#2`→`#3`-spurious rebuild). Runde 3-fixen klarerte kun timer ved inngang, ikke en som armeres i await-vinduet. |
| 5 | CONFIRM (kryss-modell-gate re-run, Sonnet) | Fix-commit `cd29d2f`: `rebuildInProgress`-flagg (satt før await, ryddet i `finally`, sjekket i `scheduleRebuild`-guarden) + test med holdbar `setAuth` som beviser rød-uten-fix. Fersk Sonnet-gate traceet alle interleavings (rejection-ordering `finally`→`.catch` probet empirisk, unsubscribe-under-await, dobbel-invokasjon strukturelt umulig, sync-status-race uoppnåelig i ekte bibliotek) og fant ingen defekt. Full suite 5941 grønn. KONVERGERT på taket (5 runder totalt). |
