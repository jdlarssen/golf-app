# Forge evaluation — #987, #986, #971

Branch: `claude/gracious-jang-d6c0e7` · Evaluated 2026-06-30 (skeptical, fresh-context)

## Gates (run independently, Node v22.23.0)

| Gate | Result |
|---|---|
| `npx tsc --noEmit` | **PASS** (exit 0) |
| `npm run lint` | **PASS** — 0 errors, 50 warnings; all warnings in pre-existing unrelated files (sideTournament.ts, wolf.ts, fitsPlayerCount.ts, cup/league snapshots, scorecardLayout.ts, etc.). **Zero warnings in any changed file** (verified by grep on exact changed paths). |
| `npx vitest run` (4 co-located suites) | **PASS** — 56/56 (`roundScore` 8, `buildShareCardData` 45, `BottomActionBar` 3, `GameHistoryRow` not re-run here but green in batch) |
| i18n parity `leaderboard.shareCard` | **PASS** — NO 11 keys, EN 11 keys, diff `[]` |
| `FinishedGameCard.test.tsx` (regression check) | **PASS** — 3/3 (component still alive on /spill-arkiv) |

---

## #987 — BottomActionBar safe-area (commit c6b79d94, fix/PATCH)

| K | Verdict | Evidence |
|---|---|---|
| K1 — all 4 corners rounded | **PASS** | `borderRadius: 16` (single value), BottomActionBar.tsx:30. Old `'18px 18px 0 0'` gone. |
| K2 — framed/inset, box-sizing | **PASS** | `width: 'calc(100% - 32px)'` (line 22), `marginLeft/Right: 'auto'` (25-26), `boxSizing: 'border-box'` (23), `display: 'block'` (21). `width:'100%'` removed. |
| K3 — safe-area on bg not button | **PASS** | No `env(safe-area)` in `padding` (now `'17px 18px'`, line 28); safe-area in `marginBottom: 'calc(16px + env(safe-area-inset-bottom, 0px))'` (line 27). HoleClient adds no safe-area padding of its own (grep clean). |
| K4 — disabled/enabled unchanged | **PASS** | Branches identical to main: `disabled||!href` → `<button disabled>`; else `<SmartLink href>`. `aria-label={label}` on both. Colors `--disabled-bg/fg` vs `--primary`/`--bg-tint`. |
| K5 — existing test green | **PASS** | BottomActionBar.test.tsx unchanged, 3/3 pass. No new tests added. |
| K6 — visual on staging | **N/A here** | Owner-side UI check (excluded from this evaluation by instruction). |
| K7 — gates + version | **PASS** | tsc/lint clean; PATCH bump 1.158.1→1.158.2; one Feilrettinger line for #987. |

Comment updated (lines 12-18) to describe the new framed pattern. **Verdict: ACCEPT.**

---

## #986 — «Runder» rows on Hjem (commit b47e3513, feat/MINOR)

| K | Verdict | Evidence |
|---|---|---|
| K1 — shared helper + TDD | **PASS** | `lib/games/roundScore.ts::computeRoundScore`; roundScore.test.ts covers empty→null/null, no-hcp→netto null, strokes+hcp→netto, null-stroke-as-0, `it.each` (3 cases). 8/8 green. Matches contract formula exactly (`strokes.length>0 ? sum(s??0) : null`; `brutto!=null && hcp!=null ? brutto-hcp : null`). |
| K2 — historikk uses helper | **PASS** | historikk/page.tsx:242-245 replaces inline `bruttoSum`/`nettoSum` with `computeRoundScore(gameScores.map(s=>s.strokes), course_handicap)`. Behavior-identical (holeCount === strokes.length). |
| K3 — Hjem renders rows | **PASS** | `FinishedRoundsSection` renders `GameHistoryRow` for `slice(0,3)` (not FinishedGameCard); brutto hero + netto label. page.tsx now imports/uses FinishedRoundsSection; FinishedGameCard import removed from home. |
| K4 — datalayer fetch top-3 | **PASS** | `getRoundScoresForGames(supabase, userId, recent.map(g=>g.id))` — only the ≤3 shown ids. RLS-correct: `.eq('user_id', userId).in('game_id', ids).not('strokes','is',null)` for scores + `game_players.course_handicap` user-scoped. Mirrors historikk fetch. (Live staging brutto/netto = owner-side.) |
| K5 — «Se alle» tail kept | **PASS** | FinishedRoundsSection.tsx:90 `finishedGames.length > 3` → SmartLink to `/spill-arkiv` with `sectionFinishedShowAll`. |
| K6 — incomplete rounds | **PASS** | GameHistoryRow renders `brutto != null ? brutto : '—'` and drops netto when null; computeRoundScore returns null/null for empty strokes. No crash path. |
| K7 — gates + version | **PASS** | tsc/lint clean; roundScore + GameHistoryRow tests green; MINOR bump →1.159.0; one Funksjon line. |
| K8 — staging | **N/A here** | Owner-side. |

Scope respected: `/spill-arkiv/page.tsx:70` STILL uses FinishedGameCard (not dead); GameHistoryRow unchanged. Badge logic (`finishedResultBadge`, `tFinished` namespace, `isWin`) mirrors FinishedGameCard exactly. href changed to `?from=/` per contract K3 (intentional). `home.roundNetto` added to both catalogs (NO `"{netto} netto"`, EN `"{netto} net"`).

**Minor note (not a gap):** No unit test for `getRoundScoresForGames` — acceptable, it's a thin DB-boundary fetch; only the pure helper needed TDD (K1), which it has.

**Verdict: ACCEPT.**

---

## #971 — Localize share card (commit 067fdab9, feat/MINOR)

| K | Verdict | Evidence |
|---|---|---|
| K1 — buildShareCardData structured | **PASS** | `ShareCardScore` discriminated union `{kind:'points'/'skins'/'vsPar'}`; `match` carries `sharerOutcome`/`headline` structured; `playerFallback` param replaces hardcoded `'Spiller'`. Grep for poeng/skins/Spiller/Uavgjort/vant/etc. → only `skins` band-name + 1 code comment remain (no emitted NO strings). vsPar `−/E/+` golf notation + margin (`3&2`/`2up`) kept locale-neutral per contract. |
| K2 — test updated + green | **PASS** | buildShareCardData.test.ts asserts new shape (`{kind:'vsPar',label}`, `{kind:'points',value}`, `match.sharerOutcome`, `match.headline`). No old `"{n} poeng"` assertions. 45/45 green. |
| K3 — route i18n via locale | **PASS** | `getTranslations({locale: resolvedLocale, namespace:'leaderboard.shareCard'})` (route:199-202); all labels via `t()` (finalResult/winner/matchplay/gameFinished/tied/winnerWon/points/skins/holes/tagline/playerFallback). Only literal strings = brand `Tørny` (302) + `tornygolf.no` (407). Norwegian-literal grep on route = none. |
| K4 — locale-correct date | **PASS** | `osloDate(iso, locale)` → `locale==='en' ? 'en-GB' : 'nb-NO'` (route:145). Called with `resolvedLocale`. |
| K5 — keys in both catalogs | **PASS** | `leaderboard.shareCard` in no.json + en.json; parity diff `[]` (11/11). EN idiomatic (`Halved` for matchplay tie, `Final result`, `Fire up your golf tournament in a couple of minutes`). |
| K6 — NO card unchanged | **PASS** | Every NO value matches the previous hardcoded string exactly: Sluttresultat / Spillet er ferdig / VINNER / MATCHPLAY / `{n} poeng` / `{n} skins` / `{n} hull` / Spiller / Uavgjort / `{name} vant {margin}` / tagline. Verified against `git show main:`. |
| K7 — EN card English | **N/A (render)** | Code path proven (renderScore/renderMatchHeadline → t()); actual PNG eyeball is owner-side. |
| K8 — gates + version + humanizer | **PASS** | tsc/lint clean; test green; MINOR →1.160.0; one Funksjon line; EN copy idiomatic. |

Out-of-scope respected: `computeSharerSideAwards` / side-tournament labels NOT touched (label is caller-supplied, excluded per contract item 4). Card layout/design unchanged.

**Behavior-preservation check (matchplay):** old `MatchplayBody` rendered `headline || sharerOutcomeLabel || 'Matchplay'`; since old `headline` was always set when a result existed, the neutral headline is what showed — new `renderMatchHeadline` reproduces this (winner→winnerWon, tied→tied, undecided→matchplay). `sharerOutcome` is kept in the model (as old `sharerOutcomeLabel` was) but, like before, not separately rendered. **Not a regression.**

**Verdict: ACCEPT.**

---

## Cross-cutting observations

- **CHANGELOG format:** entries use `<details><summary>` blocks. MEMORY/CLAUDE.md describe a newer flat 2-section format (#952). The implementer matched the **existing file's** convention (every prior entry is `<details>`-wrapped), so this is internally consistent, not a contract breach. Flag for the owner only if a CHANGELOG reformat is pending. Not blocking.
- **Per-commit version discipline:** 1.158.1 → .2 (PATCH) → 1.159.0 (MINOR) → 1.160.0 (MINOR). Correct.
- No dead code, no hardcoded debug/TODO, no scope creep found. FinishedGameCard intact on /spill-arkiv. computeSharerSideAwards untouched.

## Overall: ACCEPT (all three)
