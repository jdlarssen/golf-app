# Evaluering: fire mellomstore restanser (#1591 · #1590 · #1394 · #1395)

**PR:** #1664 · **Branch:** `claude/mellomstore-1591-1590-1394-1395` (7 commits over `origin/main`)
**Kontrakt:** `.forge/contracts/1591-1590-1394-1395-mellomstore.md`
**Evaluator:** fresh-context, skeptisk · **Dato:** 2026-08-15
**Staging:** `snwmueecmfqqdurxedxv` (torny-staging) · dev-server på `localhost:3144` fra denne worktreen

---

## Suksesskriterier

| # | Kriterium | Verdikt | Bevis |
|---|---|---|---|
| 1 | #1591: én `TIER_ACCENT` | **PASS** | `grep -rn "const TIER_ACCENT"` → 1 treff: `podiumPresentation.ts:63`. 13 podium-filer + kilden importerer den (14 filer totalt). Ingen kopier utenfor leaderboard-mappa. |
| 2 | #1591: `SLOT_HEIGHTS` + `SLOT_HEIGHTS_TALL` i `podiumPresentation.ts`, ingen lokale kopier | **PASS** | `grep -rn "const SLOT_HEIGHTS"` → kun `podiumPresentation.ts:40` og `:53`. `SLOT_HEIGHTS_TALL` brukes av nøyaktig de 5 forventede (Nassau, TexasScramble, Shamble, Patsome, TeamStableford). Doc-kommentar forklarer hvorfor (ekstra innholdslinje). |
| 3 | #1591: Nassau-medaljong 48/36 | **PASS** | `NassauPodium.tsx:285` → `place === 1 ? 48 : 36`. Alle 13 podier har nå identisk uttrykk (grep `medallionSize = `). |
| 4 | #1591: scramble-podier sorterer ikke selv | **PASS** | `ShamblePodium.tsx` og `TexasScramblePodium.tsx` leser `result.teams[0..2]` direkte; call-site-`sort` fjernet i begge. |
| 5 | #1591: Texas-compute returnerer rangert, TDD-et rødt først | **PASS** | **Probe 1:** `git checkout origin/main -- lib/scoring/modes/texasScramble.ts` → ny test feiler (`[1,2]` ≠ `[2,1]` på `texasScramble.test.ts:697`), restaurert → grønn. Shamble trengte ingen kode-endring (`shamble.ts` urørt i diffen; `teams.sort()` lå der fra før på `:250`) — kun én ny assert, som samsvarer med byggerens påstand. |
| 6 | #1591: `.teams`-konsumenter er rekkefølge-uavhengige | **PASS** | Gjennomlest: `resultSummary.ts:68`, `buildShareCardData.ts:198`, `roundReportFacts.ts:250`, `gameFinishedRecipients.ts:740-795`, `formats/texasScramble.tsx:160`. Alle mapper/aggregerer per lag eller bygger `Map` på userId — ingen leser indeks 0 som «vinner» eller antar teamNumber-rekkefølge. |
| 7 | #1590: InstallInstructionsModal + RejectModal bruker hooken | **PASS** | Begge har `const { containerRef } = useModalFocus<HTMLDivElement>(…)` + `ref={containerRef}` + `tabIndex={-1}` på `role="dialog"`-elementet. Escape-effekten beholdt i begge (hooken tar bevisst ingen callbacks). |
| 8 | #1590: RejectModal lukker på Escape | **PASS** | `PåmeldingerClient.tsx:290-303` — `cancelRef`-mønsteret gjør at lytter-effekten kjører én gang per åpning i stedet for per tegn i begrunnelses-feltet. Verifisert i nettleser (staging-punkt 3). |
| 9 | #1590: FormatGuideSheet migrert med test FØRST | **PASS** | Commit-rekkefølge: `0a24e969 test(format-guide): lock … before migrating` → `b38bb3a7 fix(a11y): route … through useModalFocus`. **Probe 4:** testen passerer også mot `origin/main`-versjonen av `FormatGuideSheet.tsx` — den er en ekte karakteriserings-test som låser oppførsel på tvers av implementasjonsbytte, ikke en etterpå-skrevet bekreftelse. |
| 10 | #1590: `tabindex="-1"`-avviket (byggerens oppgitte avvik) er reelt og riktig løst | **PASS** | Honeypot-feltet finnes: `PåmeldingerClient.tsx` — `<div aria-hidden style={{display:'none'}}><input name="website" tabIndex={-1}>` som FØRSTE felt i formen. **Probe 3:** med `origin/main`-hooken feiler både `PåmeldingerClient`-testen og hook-testen, og feilutskriften viser at jsdom faktisk fokuserer honeypot-inputen. Assertion-en er stram nok: `getByRole('textbox')` treffer bare textarea-en (honeypot ligger under `aria-hidden`, altså utenfor a11y-treet). |
| 11 | #1394: feilende handling → rollback + `inbox-action-error` | **PASS** | **Probe 2:** `git checkout origin/main -- InboxClient.tsx` → testen feiler på `getByText(/Per inviterte deg/)`. Staging-punkt 4a bekrefter i ekte nettleser. |
| 12 | #1394: `toggleProductUpdates` returnerer `ok=false` ved 0 rader/feil | **PASS** | `actions.ts` bruker `expectAffected(… .select('id'), 'toggleProductUpdates')` i `try/catch` → `{ok:false}`. `expectAffected` (`lib/supabase/affectedRows.ts`) kaster `Error` på `result.error` og `NoRowsAffectedError` på tom/`null` `data` — begge fanges. |
| 13 | #1394: øvrige kallere kompilerer med `void` | **PASS** | `markNotificationsRead`/`archiveNotifications` gikk `Promise<void>` → `Promise<boolean>`; `after()`- og page-kallerne i `admin/games/[id]/page.tsx`, `games/[id]/(home)/page.tsx`, `leaderboard/page.tsx`, `approve/page.tsx` `void`-er/ignorerer returen. `tsc --noEmit` exit 0. |
| 14 | #1394: InboxClient gjenoppretter snapshot ved `!ok` OG ved kast | **PASS** | `runOptimistic` har både `if (!result?.ok)`-grenen og `catch (err)`-grenen; begge kaller `setItems(snapshot)` + `setActionFailed(true)`. Feillinja nullstilles ved neste vellykkede handling (`if (actionFailed) setActionFailed(false)`) — verifisert på staging (4b: feillinja borte etter ekte arkivering). |
| 15 | #1395: oppfølgings-SELECT bruker SAMME user-scopede klient | **PASS** | `loadAndAuthorize` returnerer `supabase = await getServerClient()` (cookie-basert, RLS aktiv); oppfølgings-`select('submitted_at').maybeSingle()` bruker den samme variabelen — ingen admin-klient smuglet inn. RLS-nekt gir dermed ingen rad → `?error=db`. |
| 16 | #1395: RLS-påstanden i kommentaren stemmer | **PASS** | `0092_rls_policy_perf.sql:382` — `"game_players select shared game" … using (is_admin() OR is_in_game(game_id))`. `0160_game_players_creator_select.sql:40` — `"game_players creator select" … using (public.is_game_creator_or_admin(game_id))`. Begge som sitert i koden. |
| 17 | #1395: allerede avvist → `?status=rejected`, ingen nytt varsel | **PASS** | Kode: `.not('submitted_at','is',null)` på UPDATE-en; 0 rader + synlig rad med `submitted_at === null` → `revalidateTag` + redirect `?status=rejected` FØR notify-blokka. Test `#1395` låser det. **Staging-punkt 5 er den harde beviset:** SQL-orakel = nøyaktig 1 varsel etter to innsendinger. |
| 18 | #1395: `revalidateTag` på idempotent gren — riktig? | **PASS (skjønn)** | Ja. To arrangører kan avvise samtidig; taperen av kappløpet trenger fersk `game-${id}` for ikke å bli stående med et «venter på godkjenning»-kort. Kallet er billig og speiler suksess-grenen. Ingen innvending. |
| 19 | `.changes/`-notater parser | **PASS** | `node scripts/weekly-release.mjs --dry-run` exit 0 — #1394, #1395 og #1590 lander alle som Feilrettinger-linjer i `1.233.0`. #1591 er `refactor` og har korrekt ingen notatfil. |

---

## Porter

| Port | Kommando | Resultat |
|---|---|---|
| Enhetstester (kontraktens scope) | `npx vitest run "app/[locale]/games/[id]/leaderboard" lib/scoring components hooks "app/[locale]/innboks" "app/[locale]/games/[id]/approve" "app/[locale]/admin/games/[id]/signups" lib/notifications lib/games` | **236 filer / 3074 tester grønne**, exit 0 |
| Typer | `npx tsc --noEmit -p .` | exit 0 |
| Build | `npm run build` | exit 0 (`set -o pipefail`, exit-kode lest direkte — ikke via `tail`) |
| Lint (full) | `npm run lint` | exit 0 — 56 problemer, **0 errors**, alle 56 er pre-eksisterende `complexity`/`max-depth`-warnings i urørte filer |
| Lint (endrede filer) | `npx eslint` på de 14 endrede kildefilene | exit 0, ingen output |
| Changelog | `node scripts/weekly-release.mjs --dry-run` | exit 0 |

### Falsifiserbarhets-prober (kode midlertidig tilbakestilt til `origin/main`, så restaurert)

| Probe | Hva | Resultat |
|---|---|---|
| 1 | `lib/scoring/modes/texasScramble.ts` → origin/main | Ny #1591-test **RØD** (`[1,2]` ≠ `[2,1]`) → TDD-påstanden holder |
| 2 | `app/[locale]/innboks/InboxClient.tsx` → origin/main | Ny #1394-test **RØD** → rollback-testen er falsifiserbar |
| 3 | `hooks/useModalFocus.ts` → origin/main | PåmeldingerClient- **og** hook-testen **RØDE**; jsdom fokuserte honeypot-inputen → `tabindex="-1"`-skipet er nødvendig og testet |
| 4 | `components/FormatGuideSheet.tsx` → origin/main | Ny test **GRØNN** → ekte karakteriserings-test skrevet før migrering, ikke etterrasjonalisering |

Worktreen ble verifisert ren (`git status`) etter hver probe.

---

## Staging-bevis

Drevet med Playwright via Bash mot `localhost:3144` (dev-server bootet fra denne worktreen med `.env.staging.local`; `lsof -a -p <pid> -d cwd` bekreftet riktig worktree). Innlogging via service-role-mintet `email_otp`.

| Akseptansepunkt | Struktur-orakel | Feillogg | SQL-orakel |
|---|---|---|---|
| **1. #1590 FormatGuideSheet** (veiviser steg 2, «?»-knapp) | Ved åpning: `activeElement` = ✕-knappen INNE i `[role=dialog]`. 10 × Tab → alle 10 innenfor (vandrer gjennom format-kortenes `<summary>`). Escape → dialogen borte, `activeElement` tilbake på «?»-knappen (`aria-label="Spillformater, slik funker de"`). | 0 console-errors, 0 requestfailed | — (ingen skriving) |
| **2. #1590 InstallInstructionsModal** (`/no/profile` → «Installer som app») | Raden rendres (Playwright-Chromium gir `status='unsupported'`, så modalen åpnes i stedet for native prompt). Fokus lander på ✕ inne i `[role=dialog]`. 8 × Tab → alle innenfor (dialogen har kun ett fokuserbart element, så fella sykler på seg selv — korrekt). Escape → dialogen borte, fokus tilbake på «Installer som app»-raden. | 0 console-errors, 0 requestfailed | — (ingen skriving) |
| **3. #1590 RejectModal** (`/no/admin/games/a40b0552…/signups`) | Fokus lander på `textarea#reject-reason` — **honeypot-feltet ble hoppet over i ekte nettleser**. 8 × Tab sykler textarea → «Avvis påmelding» → «Avbryt», alle innenfor. Escape → dialogen borte, fokus tilbake på «Avvis». Ikke innsendt (ikke-destruktivt). | 0 console-errors, 0 requestfailed | Påmeldingen står fortsatt `pending`, `decided_at=null` etterpå |
| **4. #1394 innboks-rollback** (`/no/innboks`) | **4a blokkert arkivering** (`route.abort` på POST til `/innboks`): kortantall 2 → **2** (rullet tilbake), feillinja synlig med eksakt copy «Fikk ikke lagret — prøv igjen.». **4b ekte arkivering:** 2 → 1, feillinja borte. **4c blokkert månedsbrev-bryter:** `aria-checked` «true» → **«true»** (flippet tilbake) + feillinje. **4d ekte bryter:** → «false», feillinje borte. | 2 console-errors + 2 requestfailed — **alle forventede**: mine egne `route.abort` (`net::ERR_FAILED /innboks`) og de bevisste catch-loggene `[innboks] optimistic action failed` / `[innboks] monthly digest toggle failed`. Ingen uventede. | 4a: `users.product_updates_unsubscribed_at` uendret (`null`) under blokkeringen. 4b: `notifications?id=eq.6203cb93…&select=archived_at` → `2026-08-15T20:22:40.785+00:00` (**not null**). 4d: `product_updates_unsubscribed_at` → `2026-08-15T20:22:51.378+00:00` (**skrevet**). Alt restaurert etterpå. |
| **5. #1395 dobbel avvisning** (`/no/games/82bfb793…/approve`) | Baseline 0 varsler. Første avvisning via UI → redirect `…/approve?status=rejected`, `submitted_at` tømt, `rejection_reason` satt. Andre innsending = **re-post av den identiske server-action-POST-en** → **HTTP 303**, `x-action-redirect: /games/82bfb793…/approve?status=rejected;push` — altså **idempotent suksess, IKKE `?error=db`**. | 0 console-errors, 0 requestfailed | `notifications?kind=eq.scorecard_rejected&user_id=eq.252e1a6f…&created_at=gte.<start>` → **count = 1** (forventet 1). Orakelet er ikke tomt-vakuøst: første avvisning ga faktisk 1 rad, så migrasjon 0149 ER påført på staging og `notifications_kind_check` slipper `scorecard_rejected` gjennom. |

**Prod-vakt: 0 brudd — hver eneste `*.supabase.co`-request gikk til `snwmueecmfqqdurxedxv` (15 kall totalt: 5 + 4 + 6 over de tre driverne).** Ingen prod-ref observert på noe tidspunkt.

### Opprydding

Alt E2E-avtrykk tilbakestilt og verifisert med REST etterpå:

- `notifications` arkivert av admin: **[]** · lest av admin: **[]** (6203cb93 satt tilbake til `archived_at=null, read_at=null`)
- `notifications` med `kind=scorecard_rejected`: **[]** (den ene E2E-raden slettet, HTTP 204)
- `users.product_updates_unsubscribed_at` for admin: **null** (opprinnelig verdi)
- `game_players` i spill `82bfb793`: begge rader `submitted_at=null, approved_at=null, rejection_reason=null`
- Ventende påmelding `5bf30530`: fortsatt `pending`, urørt
- Dev-server stoppet · `git status` ren bortsett fra `.forge/`-filene

---

## Funn

Ingen av disse blokkerer. De er notert for oppfølgings-issues per «Reviewer-funn (mandatory)».

| # | Fil + kriterium | Alvorlighet | Funn |
|---|---|---|---|
| F1 | `lib/notifications/archive.ts:50,63` + `lib/notifications/markRead.ts:57` — kriterium #1394 | **Lav** | Helperne returnerer `true` for en UPDATE som traff **0 rader** — bare en PostgREST-`error` gir `false`. En RLS-drift eller feil id rapporterer altså fortsatt suksess til InboxClient, og rollbacken fyrer ikke. Dette er AGENTS.md trap 2, og #1394 sin egen verifiserings-note peker eksplisitt på `archive.ts` som en som «svelger DB-feil». **Kontrakten ba bevisst kun om «`false` ved DB-feil»**, og det verste tilfellet (samtykke-signalet i `toggleProductUpdates`) ER dekket med `expectAffected` — derfor kontrakt-etterlevende. Verdt et oppfølgings-issue: samme `expectAffected`-behandling for de interaktive innboks-skrivingene. |
| F2 | `app/[locale]/games/[id]/approve/actions.test.ts:279` — kriterium #1395 | **Lav** | #704-casen mocker bare «raden er usynlig» (`data: null`). Det realistiske RLS-nektede tilfellet — en peer som *kan* lese raden via `is_in_game()`, men ikke oppdatere den, slik at `submitted_at` fortsatt er satt — håndteres riktig av koden (faller til `?error=db`), men ingen test låser den grenen. En regresjon som endret betingelsen til `existing !== null` ville passert hele suiten. |
| F3 | `TexasScrambleView.tsx:87` + `ShambleView.tsx:130` — kriterium #1591 | **Info** | Begge viewene kopierer og sorterer fortsatt `result.teams` selv om compute-kontrakten nå garanterer rank-rekkefølge. Byggeren oppga dette som utenfor scope, og kontraktens kriterium gjelder eksplisitt podier («scramble-podier sorterer ikke selv») — begge podiene er rene. Dødt arbeid, ikke en defekt; rydd ved neste berøring. |
| F4 | `app/[locale]/innboks/InboxClient.tsx:70-90` — kriterium #1394 | **Info** | To smale kappløp: (a) `runOptimistic` legger tilbake et snapshot av HELE lista, så overlapper to handlinger og den første feiler, forkastes også den andres optimistiske endring; (b) `if (actionFailed) setActionFailed(false)` leser en closure-fanget verdi, så en suksess som ble sendt før feil-renderen landet, fjerner ikke feillinja. Begge krever to samtidige handlinger i en ikke-kritisk rydde-flate. |
| F5 | `InboxClient.tsx` + `MonthlyDigestToggle.tsx` — kriterium #1394 | **Info** | Begge emitterer `data-testid="inbox-action-error"`, og begge kan stå på `/innboks` samtidig. En framtidig test som ikke bruker `.first()` treffer Playwright/RTL strict-mode-tvetydighet. |

Ingen avvik fra kontrakt-designet ut over de fire byggeren selv oppga — og alle fire ble verifisert som reelle og riktig håndtert (kriterium 5, 9, 10 og F3).

---

## VERDICT: ACCEPT

Alle 19 suksesskriterier PASS, alle 6 porter grønne, alle 4 falsifiserbarhets-prober bekrefter at testene faktisk fanger regresjonene de påstår å fange, og alle 5 staging-punktene er verifisert ende-til-ende i ekte nettleser med SQL-orakler. Prod-vakt: 0 brudd. Staging-data fullstendig restaurert.
